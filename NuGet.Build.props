<Project>
    <!-- Imported by library projects. Packaging metadata from upstream was removed; we don't publish NuGet packages. -->
    <PropertyGroup>
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
    </PropertyGroup>
</Project>
