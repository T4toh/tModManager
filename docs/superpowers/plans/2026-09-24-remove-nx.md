# Eliminar el `.nx` file store — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reemplazar `NxFileStore` por un store de archivos sueltos por hash, hacer de las descargas originales ciudadanas de primera clase en `tModManager/Downloads`, y guiar la limpieza de datos viejos.

**Architecture:** `LooseFileStore` implementa la misma `IFileStore` (patrón `.git/objects`), así que el Synchronizer, installers y colecciones casi no cambian. El GC pasa a un barrido "borrar lo no referenciado". Las descargas se guardan con el nombre de Nexus, su ruta queda en `LibraryFile.DownloadPath`, y un `IDownloadReExtractor` repone en el store lo que falte a partir de ellas.

**Tech Stack:** C# / .NET 10, MnemonicDB 0.28.2, Avalonia 11, xunit v3 + FluentAssertions 7, NexusMods.Paths 0.22.5.

**Spec:** `docs/superpowers/specs/2026-09-24-remove-nx-design.md`

## Global Constraints

- Repo: `/Users/tatoh/Repos/Personal/cp2077-mm`, rama `feat/remove-nx` (PR 1), `feat/downloads-first-class` (PR 2, sale de PR 1), `feat/legacy-cleanup-wizard` (PR 3, sale de PR 2).
- Build en macOS: `DOTNET_ROLL_FORWARD=Major dotnet build NexusMods.App.sln -p:TreatWarningsAsErrors=true` → 0 warnings, 0 errores.
- Tests: la mayoría necesita Linux (`LinuxInterop`). Se corren en container con `$S/test.sh` (Task 0). La suite completa se compara contra el baseline de `main` con `$S/ci.sh` (fallos preexistentes del container: 26 xUnit + 2 TUnit de Backend.Tests; ver `$S/ci-main.log`).
- NuGet: versiones centralizadas en `Directory.Packages.props`; nunca `Version=` en un `<PackageReference>`. Cualquier paquete nuevo con ≥7 días publicado (regla de la organización). Este plan no agrega paquetes.
- Commits en inglés, estilo `feat:`/`fix:`/`chore:`/`test:`/`docs:`, **sin** `Co-Authored-By` (regla de la organización).
- Mensajes de log y strings de UI nuevos en español (convención del fork).
- `.globalconfig` hace error de `CS4014`, `CS8509`, `CA1069`, `CA2211`, `CA2021`: no suprimir.
- No push de tags ni releases. PRs los abre el agente; merge lo hace Tatoh.
- Cuerpos de PR terminan con la línea `🤖 Generated with [Claude Code](https://claude.com/claude-code)`.
- Datos del usuario: nada en este plan migra datos viejos. Arrancar de cero es el requisito.
- Tests no tocan el estado real del usuario: toda ruta nueva (`DownloadsSettings`) se sobreescribe a temp en `AddDefaultServicesForTesting`.

## Review Focus

1. **Escritura interrumpida o dos imports del mismo archivo a la vez**: nunca debe quedar un archivo con nombre de hash válido y contenido incompleto, y dos `BackupFiles` concurrentes del mismo hash no deben fallar. → Tests en Task 1 (`BackupFiles_HashMismatch_LeavesNoFile`, `BackupFiles_ConcurrentSameHash_BothSucceed`).
2. **GC corriendo mientras se hace backup**: un archivo recién escrito cuyo commit en la base todavía no ocurrió no debe borrarse a mitad de escritura (mismo nivel de garantía que el lock del `NxFileStore`). → Task 2 usa el lock interno; test `Gc_DoesNotDeleteTmpFilesInFlight`.
3. **Carpeta de descargas con un archivo del mismo nombre pero otro contenido** (mod actualizado con igual nombre): no se pisa. → Task 9 `Preserve_SameNameDifferentContent_GetsSuffix`.
4. **Store borrado a mano y Apply**: debe reextraer desde Descargas en vez de fallar. → Task 11 `Apply_AfterStoreWiped_ReExtractsFromDownloads`.
5. **Arranque sin `.nx` pero con `NexusMods.App/Downloads` existente** (instalación limpia en una PC que tuvo la app oficial): el asistente **no** debe aparecer. → Task 13 `Detector_NoNx_ReturnsFalse_EvenWithLegacyDownloads`.

---

## Mapa de archivos

PR 1 (store + GC):
- Create `src/NexusMods.DataModel/LooseFileStore.cs` — store por hash.
- Create `src/NexusMods.DataModel/LiveHashes.cs` — conjunto de hashes vivos (reemplaza `DataStoreReferenceMarker`).
- Modify `src/NexusMods.DataModel/GarbageCollectorRunner.cs` — barrido.
- Modify `src/NexusMods.DataModel/MissingArchiveException.cs` — ctor con varios faltantes.
- Modify `src/NexusMods.Sdk/FileStore/IFileStore.cs` — sacar `WriteLock`/`ReloadCaches`.
- Modify `src/NexusMods.DataModel/Services.cs` — registro.
- Modify `src/NexusMods.DataModel/Storage/StorageAnalyzer.cs` — tamaño del store.
- Delete `src/NexusMods.DataModel/NxFileStore.cs`, proyectos `src/NexusMods.App.GarbageCollection{,.Nx,.DataModel}` y `tests/NexusMods.App.GarbageCollection{,.Nx,.DataModel}.Tests`, código muerto de stream sources.
- Tests: `tests/NexusMods.DataModel.Tests/LooseFileStoreTests.cs`, `tests/NexusMods.DataModel.Tests/GarbageCollectorTests.cs`; adaptar `FileStoreTests.cs`, `AGameTest.cs`, `AIsolatedGameTest.cs`, benchmarks.

PR 2 (descargas):
- Create `src/NexusMods.Sdk/Library/DownloadsSettings.cs`.
- Create `src/NexusMods.Sdk/FileStore/IDownloadReExtractor.cs`.
- Create `src/NexusMods.Sdk/Library/DownloadsFolder.cs` — nombre/dedupe de archivos en Descargas (en Sdk porque lo usan Library y DataModel).
- Create `src/NexusMods.Library/DownloadReExtractor.cs`.
- Modify `LibraryFile.cs` (atributo `DownloadPath`), `AddDownloadJob.cs`, `AddLibraryFileJob.cs`, `CollectionDownloader.cs`, `InstallCollectionJob.cs`, `InstallCollectionDownloadJob.cs`, `ALoadoutSynchronizer.cs`, `StorageAnalyzer.cs`, `IStorageAnalyzer.cs`, `StorageManagerPageViewModel.cs` (+ view), `DataModelSettings.cs`, test helpers.

PR 3 (asistente + Deep Clean):
- Create `src/NexusMods.DataModel/LegacyData/LegacyDataDetector.cs`.
- Create `src/NexusMods.App.UI/Overlays/LegacyCleanup/*` (VM, interfaz, view).
- Modify `CyberpunkDeepCleanTool.cs`, `StorageAnalyzer.cs`, `MainWindowViewModel.cs`, `StorageManagerPageViewModel.cs`.
- Docs: `CLAUDE.md`, `TODO.md`.

---

## Task 0: Harness de tests en container

**Files:** solo scratchpad (`S=/private/tmp/claude-501/-Users-tatoh-Repos-Personal/20dfaf57-ac05-45f2-b2bc-a3fca85779ed/scratchpad`).

- [ ] **Step 1: Crear `$S/test.sh`** (corre un proyecto con filtro opcional dentro del container):

```bash
cat > $S/test.sh <<'EOF'
set -uo pipefail
proj="$1"; filter="${2:-}"
apt-get update -qq >/dev/null && apt-get install -y -qq rsync git libfontconfig1 >/dev/null
rsync -a --exclude bin --exclude obj --exclude .git /src/ /work/ && cd /work
git init -q && git add -A >/dev/null 2>&1 && git -c user.email=x@x -c user.name=x commit -qm x
dotnet build "$proj" 2>&1 | grep -E " error |Error\(s\)"
if [ -n "$filter" ]; then dotnet test "$proj" --no-build --filter "$filter"; else dotnet test "$proj" --no-build; fi 2>&1 | grep -E "Passed!|Failed!|\[FAIL\]|Error Message|No test is available" -A6 | head -80
EOF
```

Uso: `container run --rm -c 8 -m 12G -e DOTNET_ROLL_FORWARD=Major -v /Users/tatoh/Repos/Personal/cp2077-mm:/src:ro -v $S:/s mcr.microsoft.com/dotnet/sdk:10.0 bash /s/test.sh tests/NexusMods.DataModel.Tests "FullyQualifiedName~LooseFileStoreTests"`

De acá en adelante, `RUN <proyecto> <filtro>` abrevia ese comando.

- [ ] **Step 2: Verificar el harness con un test existente**

Run: `RUN tests/NexusMods.DataModel.Tests "FullyQualifiedName~LibraryServiceTests"`
Expected: `Passed!`

---

# PR 1 — Store por hash, GC por barrido, fuera `.nx`

## Task 1: `LooseFileStore`

**Files:**
- Create: `src/NexusMods.DataModel/LooseFileStore.cs`
- Modify: `src/NexusMods.DataModel/MissingArchiveException.cs`
- Test: `tests/NexusMods.DataModel.Tests/LooseFileStoreTests.cs`

**Interfaces:**
- Produces: `public sealed class LooseFileStore : IFileStore` con ctor DI `(ILogger<LooseFileStore>, ISettingsManager, IFileSystem)` y ctor de test `internal LooseFileStore(ILogger<LooseFileStore>, AbsolutePath root)`; `public AbsolutePath PathFor(Hash)` (público: lo usan tests de otros proyectos); `internal int DeleteAllExcept(IReadOnlySet<Hash> live)` (Task 2 la implementa); `internal AsyncFriendlyReaderWriterLock Lock`.
- Produces: `MissingArchiveException(IReadOnlyList<(Hash Hash, AbsolutePath Dest)> missing)`.

- [ ] **Step 1: Escribir los tests que fallan**

`tests/NexusMods.DataModel.Tests/LooseFileStoreTests.cs`:

```csharp
using System.Text;
using FluentAssertions;
using Microsoft.Extensions.Logging.Abstractions;
using NexusMods.Hashing.xxHash3;
using NexusMods.Paths;
using NexusMods.Sdk.FileStore;
using NexusMods.Sdk.IO;
using Xunit;

namespace NexusMods.DataModel.Tests;

public class LooseFileStoreTests : IDisposable
{
    private readonly AbsolutePath _root = FileSystem.Shared.GetKnownPath(KnownPath.TempDirectory)
        .Combine($"LooseFileStoreTests-{Guid.NewGuid():N}");
    private readonly AbsolutePath _out;
    private readonly LooseFileStore _store;

    public LooseFileStoreTests()
    {
        _out = _root.Combine("out");
        _store = new LooseFileStore(NullLogger<LooseFileStore>.Instance, _root.Combine("store"));
    }

    public void Dispose() { if (_root.DirectoryExists()) _root.DeleteDirectory(true); }

    private static ArchivedFileEntry Entry(string text, Hash? hashOverride = null)
    {
        var bytes = Encoding.UTF8.GetBytes(text);
        return new ArchivedFileEntry(new MemoryStreamFactory("x", new MemoryStream(bytes)), hashOverride ?? bytes.xxHash3(), Size.FromLong(bytes.Length));
    }

    [Fact]
    public async Task BackupFiles_ThenHaveFileAndLoad()
    {
        var e = Entry("hola");
        (await _store.HaveFile(e.Hash)).Should().BeFalse();

        await _store.BackupFiles([e]);

        (await _store.HaveFile(e.Hash)).Should().BeTrue();
        (await _store.Load(e.Hash)).Should().Equal(Encoding.UTF8.GetBytes("hola"));
        _store.PathFor(e.Hash).Parent.FileName.ToString().Should().Be(e.Hash.ToHex()[..2]);
    }

    [Fact]
    public async Task BackupFiles_SameHashTwice_KeepsOneFile()
    {
        var e = Entry("dup");
        await _store.BackupFiles([e, e]);
        await _store.BackupFiles([e]);
        _store.PathFor(e.Hash).Parent.EnumerateFiles("*", recursive: false).Should().ContainSingle();
    }

    [Fact]
    public async Task BackupFiles_HashMismatch_LeavesNoFile()
    {
        var bad = Entry("contenido", hashOverride: Hash.From(42));
        var act = async () => await _store.BackupFiles([bad]);
        await act.Should().ThrowAsync<InvalidDataException>();
        (await _store.HaveFile(Hash.From(42))).Should().BeFalse();
        _store.PathFor(Hash.From(42)).Parent.EnumerateFiles("*", recursive: false).Should().BeEmpty("no .tmp leftovers");
    }

    [Fact]
    public async Task BackupFiles_ConcurrentSameHash_BothSucceed()
    {
        var a = Entry("carrera");
        var b = Entry("carrera");
        await Task.WhenAll(_store.BackupFiles([a]), _store.BackupFiles([b]));
        (await _store.Load(a.Hash)).Should().Equal(Encoding.UTF8.GetBytes("carrera"));
    }

    [Fact]
    public async Task ExtractFiles_CopiesAndCreatesEmptyFiles()
    {
        var e = Entry("mod");
        await _store.BackupFiles([e]);
        var empty = Array.Empty<byte>().xxHash3();
        var dest1 = _out.Combine("a/b/file.txt");
        var dest2 = _out.Combine("c/empty.txt");

        await _store.ExtractFiles([(e.Hash, dest1), (empty, dest2)]);

        (await dest1.ReadAllTextAsync()).Should().Be("mod");
        dest2.FileExists.Should().BeTrue();
        dest2.FileInfo.Size.Should().Be(Size.Zero);
    }

    [Fact]
    public async Task ExtractFiles_MissingHash_ThrowsBeforeWritingAnything()
    {
        var present = Entry("ok");
        await _store.BackupFiles([present]);
        var destOk = _out.Combine("ok.txt");

        var act = async () => await _store.ExtractFiles([(present.Hash, destOk), (Hash.From(7), _out.Combine("missing.txt"))]);

        (await act.Should().ThrowAsync<MissingArchiveException>()).WithMessage("*1*");
        destOk.FileExists.Should().BeFalse();
    }

    [Fact]
    public async Task GetFileStream_IsSeekable()
    {
        var e = Entry("stream");
        await _store.BackupFiles([e]);
        await using var s = await _store.GetFileStream(e.Hash);
        s.CanSeek.Should().BeTrue();
        s.Length.Should().Be(6);
    }
}
```

- [ ] **Step 2: Correr y verificar que falla**

Run: `RUN tests/NexusMods.DataModel.Tests "FullyQualifiedName~LooseFileStoreTests"`
Expected: error de compilación `The type or namespace name 'LooseFileStore' could not be found`.

- [ ] **Step 3: Agregar el ctor múltiple a `MissingArchiveException`**

En `src/NexusMods.DataModel/MissingArchiveException.cs`, después del ctor con `path`:

```csharp
    /// <summary>
    /// Constructor for several missing files; the message lists the first ten.
    /// </summary>
    public MissingArchiveException(IReadOnlyList<(Hash Hash, AbsolutePath Dest)> missing)
        : base($"Missing {missing.Count} file(s) in the file store: " +
               string.Join(", ", missing.Take(10).Select(m => $"{m.Hash.ToHex()} → {m.Dest}"))) { }
```

- [ ] **Step 4: Implementar `LooseFileStore`**

`src/NexusMods.DataModel/LooseFileStore.cs`:

```csharp
using Microsoft.Extensions.Logging;
using NexusMods.Hashing.xxHash3;
using NexusMods.Paths;
using NexusMods.Sdk.FileStore;
using NexusMods.Sdk.Settings;
using NexusMods.Sdk.Threading;

namespace NexusMods.DataModel;

/// <summary>
/// Content-addressed file store: one plain file per xxHash3, under a two-character prefix folder
/// (<c>ab/abcd…</c>), the same layout as <c>.git/objects</c>.
/// </summary>
public sealed class LooseFileStore : IFileStore
{
    private static readonly Hash EmptyFile = Array.Empty<byte>().xxHash3();
    private const string TmpMarker = ".tmp-";

    private readonly ILogger<LooseFileStore> _logger;
    private readonly AbsolutePath _root;

    /// <summary>
    /// Backups take the read lock and garbage collection the write lock, so GC never deletes a
    /// file while it is being written. Same guarantee the Nx store gave.
    /// </summary>
    internal AsyncFriendlyReaderWriterLock Lock { get; } = new();

    public LooseFileStore(ILogger<LooseFileStore> logger, ISettingsManager settingsManager, IFileSystem fileSystem)
        : this(logger, settingsManager.Get<DataModelSettings>().ArchiveLocations[0].ToPath(fileSystem)) { }

    internal LooseFileStore(ILogger<LooseFileStore> logger, AbsolutePath root)
    {
        _logger = logger;
        _root = root;
        _root.CreateDirectory();
    }

    public AbsolutePath PathFor(Hash hash)
    {
        var hex = hash.ToHex();
        return _root.Combine(hex[..2]).Combine(hex);
    }

    /// <inheritdoc />
    public ValueTask<bool> HaveFile(Hash hash) => ValueTask.FromResult(PathFor(hash).FileExists);

    /// <inheritdoc />
    /// <remarks><paramref name="deduplicate"/> is ignored: a content-addressed store always deduplicates.</remarks>
    public async Task BackupFiles(IEnumerable<ArchivedFileEntry> backups, bool deduplicate = true, CancellationToken token = default)
    {
        using var _ = Lock.ReadLock();
        var entries = backups.DistinctBy(x => x.Hash).Where(x => !PathFor(x.Hash).FileExists).ToArray();
        await Parallel.ForEachAsync(entries, token, async (entry, ct) =>
        {
            var dest = PathFor(entry.Hash);
            dest.Parent.CreateDirectory();
            var tmp = dest.Parent.Combine($"{dest.FileName}{TmpMarker}{Guid.NewGuid():N}");
            try
            {
                await using (var src = await entry.StreamFactory.GetStreamAsync())
                await using (var dst = tmp.Create())
                    await src.CopyToAsync(dst, ct);

                Hash actual;
                await using (var check = tmp.Read())
                    actual = await check.xxHash3Async(token: ct);
                if (actual != entry.Hash)
                    throw new InvalidDataException($"Hash mismatch backing up {entry.Hash.ToHex()}: content hashes to {actual.ToHex()}");

                // Another writer may have finished the same hash first; identical content, so overwriting is harmless.
                File.Move(tmp.ToString(), dest.ToString(), overwrite: true);
            }
            finally
            {
                if (tmp.FileExists) tmp.Delete();
            }
        });
        if (entries.Length > 0) _logger.LogDebug("Guardados {Count} archivos en el store", entries.Length);
    }

    /// <inheritdoc />
    public async Task ExtractFiles(IEnumerable<(Hash Hash, AbsolutePath Dest)> files, CancellationToken token = default, Action<(int Current, int Max)>? progress = null)
    {
        var list = files.ToArray();
        var missing = list.Where(f => f.Hash != EmptyFile && !PathFor(f.Hash).FileExists).ToArray();
        if (missing.Length > 0) throw new MissingArchiveException(missing);

        var done = 0;
        await Parallel.ForEachAsync(list, token, async (f, ct) =>
        {
            f.Dest.Parent.CreateDirectory();
            if (f.Hash == EmptyFile)
            {
                f.Dest.Create().Dispose();
            }
            else
            {
                await using var src = PathFor(f.Hash).Read();
                await using var dst = f.Dest.Create();
                await src.CopyToAsync(dst, ct);
            }

            var n = Interlocked.Increment(ref done);
            if (n % 100 == 0 || n == list.Length) progress?.Invoke((n, list.Length));
        });
    }

    /// <inheritdoc />
    public async Task<Dictionary<Hash, byte[]>> ExtractFiles(IEnumerable<Hash> files, CancellationToken token = default)
    {
        var result = new Dictionary<Hash, byte[]>();
        foreach (var hash in files.Distinct())
            result[hash] = await Load(hash, token);
        return result;
    }

    /// <inheritdoc />
    public Task<Stream> GetFileStream(Hash hash, CancellationToken token = default)
    {
        if (hash == EmptyFile) return Task.FromResult<Stream>(new MemoryStream([], writable: false));
        var path = PathFor(hash);
        if (!path.FileExists) throw new MissingArchiveException(hash);
        return Task.FromResult(path.Read());
    }

    /// <inheritdoc />
    public async Task<byte[]> Load(Hash hash, CancellationToken token = default)
    {
        if (hash == EmptyFile) return [];
        var path = PathFor(hash);
        if (!path.FileExists) throw new MissingArchiveException(hash);
        return await path.ReadAllBytesAsync(token);
    }

    /// <summary>
    /// Deletes every stored file whose hash is not in <paramref name="live"/>, plus stale temp files.
    /// Returns the number of deleted files. Implemented in Task 2.
    /// </summary>
    internal int DeleteAllExcept(IReadOnlySet<Hash> live) => throw new NotImplementedException();
}
```

- [ ] **Step 5: Correr los tests**

Run: `RUN tests/NexusMods.DataModel.Tests "FullyQualifiedName~LooseFileStoreTests"`
Expected: `Passed! - Failed: 0, Passed: 7`

`xxHash3Async` sobre `Stream` es la extensión de `NexusMods.Hashing.xxHash3` (ya usada en `FileStoreTests`); si no acepta `token:`, llamarla sin argumentos.

- [ ] **Step 6: Commit**

```bash
git add src/NexusMods.DataModel/LooseFileStore.cs src/NexusMods.DataModel/MissingArchiveException.cs tests/NexusMods.DataModel.Tests/LooseFileStoreTests.cs
git commit -m "feat: add LooseFileStore, a content-addressed file store"
```

## Task 2: GC por barrido y hashes vivos

**Files:**
- Create: `src/NexusMods.DataModel/LiveHashes.cs`
- Modify: `src/NexusMods.DataModel/LooseFileStore.cs` (`DeleteAllExcept`)
- Modify: `src/NexusMods.DataModel/GarbageCollectorRunner.cs`
- Test: `tests/NexusMods.DataModel.Tests/LooseFileStoreTests.cs` (casos de borrado), `tests/NexusMods.DataModel.Tests/GarbageCollectorTests.cs`

**Interfaces:**
- Consumes: `LooseFileStore.PathFor`, `LooseFileStore.Lock` (Task 1).
- Produces: `public static class LiveHashes { public static HashSet<Hash> Collect(IDb db); }`; `GarbageCollectorRunner(LooseFileStore store, IConnection connection, ILogger<GarbageCollectorRunner> logger)`.

- [ ] **Step 1: Tests de `DeleteAllExcept`** (agregar a `LooseFileStoreTests`):

```csharp
    [Fact]
    public async Task DeleteAllExcept_KeepsLiveDeletesRest()
    {
        var keep = Entry("vivo");
        var drop = Entry("muerto");
        await _store.BackupFiles([keep, drop]);

        var deleted = _store.DeleteAllExcept(new HashSet<Hash> { keep.Hash });

        deleted.Should().Be(1);
        (await _store.HaveFile(keep.Hash)).Should().BeTrue();
        (await _store.HaveFile(drop.Hash)).Should().BeFalse();
    }

    [Fact]
    public void Gc_DoesNotDeleteTmpFilesInFlight()
    {
        // A temp file younger than the grace period belongs to a backup still running in another process step.
        var tmp = _store.PathFor(Hash.From(99)).Parent.Combine("0000000000000063.tmp-abc");
        tmp.Parent.CreateDirectory();
        tmp.Create().Dispose();

        _store.DeleteAllExcept(new HashSet<Hash>());

        tmp.FileExists.Should().BeTrue();
    }
```

- [ ] **Step 2: Test de integración del GC**

`tests/NexusMods.DataModel.Tests/GarbageCollectorTests.cs` (porta `FullSystemTest` del proyecto de tests del GC viejo; `DataModel.Tests` ya tiene `Startup` con juego stubbeado vía `ACyberpunkIsolatedGameTest`):

```csharp
using FluentAssertions;
using Microsoft.Extensions.DependencyInjection;
using NexusMods.Abstractions.GC;
using NexusMods.Games.TestFramework;
using NexusMods.Hashing.xxHash3;
using NexusMods.Paths;
using NexusMods.Sdk.FileStore;
using Xunit;

namespace NexusMods.DataModel.Tests;

public class GarbageCollectorTests(ITestOutputHelper helper) : ACyberpunkIsolatedGameTest<GarbageCollectorTests>(helper)
{
    [Fact]
    public async Task DeletingLoadout_RemovesOnlyItsUnsharedFiles()
    {
        var store = ServiceProvider.GetRequiredService<IFileStore>();
        var gc = ServiceProvider.GetRequiredService<IGarbageCollectorRunner>();
        var loadout1 = await CreateLoadout();
        var loadout2 = await CreateLoadout();

        List<Hash> shared1, shared2, only1, only2;
        using (var tx = Connection.BeginTransaction())
        {
            shared1 = await AddModAsync(tx, new RelativePath[] { "shared1.txt", "shared2.txt" }, loadout1, "Shared");
            shared2 = await AddModAsync(tx, new RelativePath[] { "shared1.txt", "shared2.txt" }, loadout2, "Shared");
            only1 = await AddModAsync(tx, new RelativePath[] { "l1_a.txt", "l1_b.txt" }, loadout1, "L1");
            only2 = await AddModAsync(tx, new RelativePath[] { "l2_a.txt", "l2_b.txt" }, loadout2, "L2");
            await tx.Commit();
        }

        gc.Run();
        foreach (var h in shared1.Concat(shared2).Concat(only1).Concat(only2))
            (await store.HaveFile(h)).Should().BeTrue("everything is referenced");

        await DeleteLoadoutAsync(loadout2, GarbageCollectorRunMode.RunSynchronously);

        foreach (var h in shared1.Concat(only1))
            (await store.HaveFile(h)).Should().BeTrue();
        foreach (var h in only2)
            (await store.HaveFile(h)).Should().BeFalse();
    }
}
```

Nota: `AddModAsync` pasa a devolver `List<Hash>` en Task 4. Hasta entonces este test no compila: ejecutar Step 3 de este task solo con el filtro `LooseFileStoreTests`, y el test de integración en Task 4 Step 4.

- [ ] **Step 3: Correr y verificar que fallan**

Run: `RUN tests/NexusMods.DataModel.Tests "FullyQualifiedName~LooseFileStoreTests"` (con `GarbageCollectorTests.cs` todavía sin agregar al build: crearlo en Task 4)
Expected: los dos tests nuevos fallan con `NotImplementedException`.

- [ ] **Step 4: Implementar `DeleteAllExcept`** (reemplaza el stub en `LooseFileStore`):

```csharp
    private static readonly TimeSpan TmpGracePeriod = TimeSpan.FromHours(1);

    internal int DeleteAllExcept(IReadOnlySet<Hash> live)
    {
        using var _ = Lock.WriteLock();
        var deleted = 0;
        foreach (var file in _root.EnumerateFiles("*", recursive: true))
        {
            var name = file.FileName.ToString();
            var tmpIdx = name.IndexOf(TmpMarker, StringComparison.Ordinal);
            if (tmpIdx >= 0)
            {
                // ponytail: stale temp files from a crash are swept after an hour; in-flight ones are younger
                if (DateTime.UtcNow - File.GetLastWriteTimeUtc(file.ToString()) > TmpGracePeriod)
                {
                    file.Delete();
                    deleted++;
                }
                continue;
            }

            if (name.Length != 16) continue; // not ours
            if (live.Contains(Hash.FromHex(name))) continue;
            file.Delete();
            deleted++;
        }
        return deleted;
    }
```

- [ ] **Step 5: `LiveHashes`**

`src/NexusMods.DataModel/LiveHashes.cs`:

```csharp
using NexusMods.Abstractions.Loadouts;
using NexusMods.Hashing.xxHash3;
using NexusMods.MnemonicDB.Abstractions;
using NexusMods.Sdk.Library;
using NexusMods.Sdk.Loadouts;

namespace NexusMods.DataModel;

/// <summary>
/// Hashes that must stay in the file store: files of valid loadouts, every library file
/// (archive entries and loose top-level files) and game file backups.
/// </summary>
public static class LiveHashes
{
    public static HashSet<Hash> Collect(IDb db)
    {
        var live = new HashSet<Hash>();
        var loadoutValid = new Dictionary<LoadoutId, bool>();

        foreach (var file in LoadoutFile.All(db))
        {
            var item = new LoadoutItem.ReadOnly(db, file.Id);
            if (!loadoutValid.TryGetValue(item.LoadoutId, out var valid))
                loadoutValid[item.LoadoutId] = valid = item.Loadout.IsValid();
            if (valid) live.Add(file.Hash);
        }

        foreach (var file in LibraryFile.All(db))
            live.Add(file.Hash);

        foreach (var backup in GameBackedUpFile.All(db))
            live.Add(backup.Hash);

        return live;
    }
}
```

- [ ] **Step 6: Reescribir `GarbageCollectorRunner`**

`src/NexusMods.DataModel/GarbageCollectorRunner.cs`, reemplazar la clase completa (conservar `RunWithMode` tal cual):

```csharp
using Microsoft.Extensions.Logging;
using NexusMods.Abstractions.GC;
using NexusMods.MnemonicDB.Abstractions;
using NexusMods.Sdk;

namespace NexusMods.DataModel;

/// <inheritdoc />
public class GarbageCollectorRunner(LooseFileStore store, IConnection connection, ILogger<GarbageCollectorRunner> logger) : IGarbageCollectorRunner
{
    /// <inheritdoc />
    public void Run()
    {
        var live = LiveHashes.Collect(connection.Db);
        var deleted = store.DeleteAllExcept(live);
        logger.LogInformation("GC: {Deleted} archivos sin referencia borrados del store ({Live} vivos)", deleted, live.Count);
    }

    /// <inheritdoc />
    public Task RunAsync() => Task.Run(Run);

    /// <inheritdoc />
    public async Task RunWithMode(GarbageCollectorRunMode gcRunMode)
    {
        switch (gcRunMode)
        {
            case GarbageCollectorRunMode.RunSynchronously:
                Run();
                break;
            case GarbageCollectorRunMode.RunAsyncInBackground:
                RunAsync().FireAndForget(logger);
                break;
            case GarbageCollectorRunMode.RunAsynchronously:
                await RunAsync();
                break;
            case GarbageCollectorRunMode.DoNotRun:
                break;
            default:
                throw new ArgumentOutOfRangeException(nameof(gcRunMode), gcRunMode, null);
        }
    }
}
```

(`FireAndForget` viene de `NexusMods.CrossPlatform` en el archivo actual: conservar ese `using` si el build lo pide.)

- [ ] **Step 7: Correr los tests del store**

Run: `RUN tests/NexusMods.DataModel.Tests "FullyQualifiedName~LooseFileStoreTests"`
Expected: `Passed! - Failed: 0, Passed: 9`

- [ ] **Step 8: Commit**

```bash
git add src/NexusMods.DataModel/LiveHashes.cs src/NexusMods.DataModel/LooseFileStore.cs src/NexusMods.DataModel/GarbageCollectorRunner.cs tests/NexusMods.DataModel.Tests/LooseFileStoreTests.cs
git commit -m "feat: sweep garbage collector over the loose file store"
```

## Task 3: Cablear el store nuevo y achicar `IFileStore`

**Files:**
- Modify: `src/NexusMods.Sdk/FileStore/IFileStore.cs` (borrar `WriteLock()` y `ReloadCaches()` con sus doc comments; borrar `using NexusMods.Sdk.Threading;` si queda sin uso)
- Modify: `src/NexusMods.DataModel/Services.cs:84-89`
- Modify: `src/NexusMods.DataModel/Storage/StorageAnalyzer.cs` (`GetStorageStatsAsync`, `DeleteArchivesAsync`)
- Modify: `benchmarks/NexusMods.Benchmarks/**/DummyFileStore.cs`

**Interfaces:**
- Consumes: `LooseFileStore`, `GarbageCollectorRunner` (Tasks 1-2).

- [ ] **Step 1: Registro DI** — en `Services.cs` reemplazar el bloque "File Store" + "Readonly stream source":

```csharp
        // File Store
        coll.AddAllSingleton<IFileStore, LooseFileStore>();
```

(Se borran las tres líneas de `IReadOnlyStreamSource`/`GameFileStreamSource`/`IStreamSourceDispatcher`; sus clases se borran en Task 5.)

- [ ] **Step 2: `StorageAnalyzer`** — el tamaño del store cuenta todos los archivos bajo la ubicación, no `*.nx`:

```csharp
        var archivesSize = settings.ArchiveLocations
            .Select(loc => loc.ToPath(_fileSystem))
            .Where(dir => dir.DirectoryExists())
            .SelectMany(dir => dir.EnumerateFiles("*", recursive: true))
            .Aggregate(0UL, (acc, file) => acc + file.FileInfo.Size.Value);
```

y `DeleteArchivesAsync` borra el contenido completo de la ubicación (se reconstruye desde Descargas en PR 2):

```csharp
    public Task DeleteArchivesAsync(CancellationToken cancellationToken = default)
    {
        var settings = _settingsManager.Get<DataModelSettings>();
        foreach (var loc in settings.ArchiveLocations)
        {
            var dir = loc.ToPath(_fileSystem);
            if (!dir.DirectoryExists()) continue;
            foreach (var sub in dir.EnumerateDirectories())
                sub.DeleteDirectory(recursive: true);
        }
        return Task.CompletedTask;
    }
```

- [ ] **Step 3: `DummyFileStore` del benchmark** — borrar sus implementaciones de `WriteLock` y `ReloadCaches`.

- [ ] **Step 4: Build**

Run: `DOTNET_ROLL_FORWARD=Major dotnet build NexusMods.App.sln -p:TreatWarningsAsErrors=true 2>&1 | grep -E " error |Error\(s\)|Warning\(s\)" | sort -u`
Expected: los únicos errores son de `NxFileStore.cs`, proyectos `App.GarbageCollection*` y los helpers de test que usan `NxFileStore` (se resuelven en Tasks 4-5). Anotar la lista.

- [ ] **Step 5: Commit** (el build completo queda roto hasta Task 5; commit igual para revisar por partes)

```bash
git add -A src/NexusMods.Sdk/FileStore/IFileStore.cs src/NexusMods.DataModel/Services.cs src/NexusMods.DataModel/Storage/StorageAnalyzer.cs benchmarks
git commit -m "refactor: wire LooseFileStore, drop WriteLock/ReloadCaches from IFileStore"
```

## Task 4: Adaptar helpers y tests existentes

**Files:**
- Modify: `tests/Games/NexusMods.Games.TestFramework/AGameTest.cs:182-223,327-334`
- Modify: `tests/Games/NexusMods.Games.TestFramework/AIsolatedGameTest.cs:292-339,475-482`
- Modify: `tests/NexusMods.DataModel.Tests/FileStoreTests.cs` (test `Test_ArchiveValidation`)
- Create: `tests/NexusMods.DataModel.Tests/GarbageCollectorTests.cs` (código de Task 2 Step 2)

- [ ] **Step 1: `AddModAsync` devuelve solo los hashes** — en ambos helpers cambiar la firma a:

```csharp
    public async Task<List<Hash>> AddModAsync(ITransaction tx, IEnumerable<RelativePath> paths, LoadoutId loadoutId, string modName, LibraryArchive.ReadOnly? libraryArchive = null)
```

(en `AIsolatedGameTest` conservar el formato multilínea de parámetros), el `return` final a `return hashes;`, y borrar el método privado `GetArchivePath` completo. Los llamadores en `GeneralModManagementTests.cs:48,65` ignoran el resultado: no cambian.

- [ ] **Step 2: `Test_ArchiveValidation`** — el store nuevo lanza `InvalidDataException` ante hash incorrecto:

```csharp
        var act = async () => await _fileStore.BackupFiles([entry], deduplicate: false);
        await act.Should().ThrowAsync<InvalidDataException>(because: "the content does not hash to the declared hash");
```

- [ ] **Step 3: Crear `GarbageCollectorTests.cs`** con el código de Task 2 Step 2.

- [ ] **Step 4: Correr**

Run: `RUN tests/NexusMods.DataModel.Tests "FullyQualifiedName~GarbageCollectorTests|FullyQualifiedName~FileStoreTests|FullyQualifiedName~LooseFileStoreTests"`
Expected: `Passed!` (los proyectos GC viejos todavía existen y pueden no compilar; si bloquean el build del test, adelantar Task 5 Step 1-2).

- [ ] **Step 5: Commit**

```bash
git add tests
git commit -m "test: adapt helpers and file store tests to LooseFileStore"
```

## Task 5: Borrar `.nx`, el GC viejo y código muerto

**Files:**
- Delete: `src/NexusMods.DataModel/NxFileStore.cs`, `src/NexusMods.App.GarbageCollection/`, `src/NexusMods.App.GarbageCollection.Nx/`, `src/NexusMods.App.GarbageCollection.DataModel/`, `tests/NexusMods.App.GarbageCollection.Tests/`, `tests/NexusMods.App.GarbageCollection.Nx.Tests/`, `tests/NexusMods.App.GarbageCollection.DataModel.Tests/`
- Delete (código muerto, sin consumidores): `IStreamSourceDispatcher.cs`, `IReadOnlyStreamSource.cs`, `SourcePriority.cs` en `src/NexusMods.Sdk/FileStore/`; `StreamSourceDispatcher`, `GameFileStreamSource` (en `src/NexusMods.DataModel/`); `src/NexusMods.Sdk/IO/SourceDispatcherStreamFactory.cs`; `FileStoreStreamLoader` (`src/NexusMods.Sdk/Resources/IO/`)
- Modify: `NexusMods.App.sln`, `src/NexusMods.DataModel/NexusMods.DataModel.csproj`, `Directory.Packages.props`, cualquier `.csproj` que referencie `NexusMods.Archives.Nx` o `NexusMods.Paths.Extensions.Nx`

- [ ] **Step 1: Sacar proyectos de la solución y borrarlos**

```bash
for p in src/NexusMods.App.GarbageCollection src/NexusMods.App.GarbageCollection.Nx src/NexusMods.App.GarbageCollection.DataModel tests/NexusMods.App.GarbageCollection.Tests tests/NexusMods.App.GarbageCollection.Nx.Tests tests/NexusMods.App.GarbageCollection.DataModel.Tests; do
  dotnet sln NexusMods.App.sln remove $p/$(basename $p).csproj && git rm -rq $p
done
git rm -q src/NexusMods.DataModel/NxFileStore.cs
```

- [ ] **Step 2: Referencias** — en `src/NexusMods.DataModel/NexusMods.DataModel.csproj` borrar el `ProjectReference` a `NexusMods.App.GarbageCollection.DataModel` y el `InternalsVisibleTo` de `NexusMods.App.GarbageCollection.DataModel.Tests`. Luego:

```bash
grep -rln "NexusMods.Archives.Nx\|NexusMods.Paths.Extensions.Nx" --include='*.csproj' src tests benchmarks
```

borrar esos `PackageReference`, y en `Directory.Packages.props` borrar las `PackageVersion` de `NexusMods.Archives.Nx` y `NexusMods.Paths.Extensions.Nx`.

- [ ] **Step 3: Código muerto**

```bash
grep -rln "IStreamSourceDispatcher\|IReadOnlyStreamSource\|SourceDispatcherStreamFactory\|FileStoreStreamLoader\|GameFileStreamSource\|SourcePriority" src tests benchmarks --include='*.cs' | grep -v /obj/
```

Borrar los archivos que solo definen esos tipos. Si algún archivo con otros usos los menciona, borrar solo esas líneas. `FileStoreLoader` (usado por imágenes de FOMOD, `ImagePipelines.cs:82,244`) **se queda**: usa `IFileStore.GetFileStream`, que existe.

- [ ] **Step 4: `using` sobrantes** — `grep -rn "using NexusMods.Archives.Nx\|using NexusMods.Paths.Extensions.Nx\|using NexusMods.App.GarbageCollection" src tests benchmarks --include='*.cs' | grep -v /obj/` → borrar esas líneas.

- [ ] **Step 5: Build limpio**

Run: `DOTNET_ROLL_FORWARD=Major dotnet build NexusMods.App.sln -p:TreatWarningsAsErrors=true 2>&1 | grep -E " error |Error\(s\)|Warning\(s\)" | sort -u`
Expected: `0 Warning(s)`, `0 Error(s)`.

- [ ] **Step 6: Suite completa vs `main`**

Run:
```bash
container run --rm -c 8 -m 12G -e DOTNET_ROLL_FORWARD=Major -v /Users/tatoh/Repos/Personal/cp2077-mm:/src:ro -v $S:/s mcr.microsoft.com/dotnet/sdk:10.0 bash /s/ci.sh > $S/ci-pr1.log 2>&1
f(){ grep '\[FAIL\]' $1 | sed -E 's/^\[xUnit[^]]*\] *//; s/ \[FAIL\]//; s/^ *//' | sort -u; }; diff <(f $S/ci-main.log) <(f $S/ci-pr1.log)
```
Expected: sin fallos nuevos. Totales: los proyectos GC desaparecen (−3 proyectos) y `DataModel.Tests` suma los tests nuevos.

- [ ] **Step 7: Commit + PR 1**

```bash
git add -A
git commit -m "chore: remove the .nx file store, the Nx garbage collector and dead stream sources"
git push -u origin feat/remove-nx
gh pr create --title "feat: replace the .nx file store with a loose content-addressed store" --body "Reemplaza NxFileStore por LooseFileStore (Archives/ab/<hash>), GC por barrido (LiveHashes), fuera los 3 proyectos GarbageCollection, NexusMods.Archives.Nx y Paths.Extensions.Nx. PR 1 de 3: no lee datos .nx viejos, no publicar hasta PR 3 + prueba punta a punta. Verificación: build 0/0, suite en container igual a main. Spec: docs/superpowers/specs/2026-09-24-remove-nx-design.md"
```

---

# PR 2 — Descargas de primera clase

Rama: `git checkout -b feat/downloads-first-class` desde `feat/remove-nx`. PR con base `feat/remove-nx`.

## Task 6: `DownloadsSettings` y ruta única

**Files:**
- Create: `src/NexusMods.Sdk/Library/DownloadsSettings.cs`
- Modify: `src/NexusMods.DataModel/DataModelSettings.cs` (borrar `DownloadsFolder` y `officialDirectoryName`)
- Modify: `src/NexusMods.DataModel/Services.cs` (registrar settings)
- Modify: `src/NexusMods.DataModel/Storage/StorageAnalyzer.cs` (leer `DownloadsSettings`)
- Modify: el `AddDefaultServicesForTesting` (buscar con `grep -rn "static IServiceCollection AddDefaultServicesForTesting" src tests`) para sobreescribir la ruta a temp

**Interfaces:**
- Produces: `public record DownloadsSettings : ISettings { public ConfigurablePath Folder { get; init; } }`, default `XDG_DATA_HOME/tModManager/Downloads`.

- [ ] **Step 1: Crear `DownloadsSettings`**

```csharp
using JetBrains.Annotations;
using Microsoft.Extensions.DependencyInjection;
using NexusMods.Paths;
using NexusMods.Sdk.Settings;

namespace NexusMods.Sdk.Library;

/// <summary>
/// Where original downloads (the archives exactly as Nexus serves them) are kept.
/// </summary>
[PublicAPI]
public record DownloadsSettings : ISettings
{
    public ConfigurablePath Folder { get; init; }

    public static ISettingsBuilder Configure(ISettingsBuilder settingsBuilder) => settingsBuilder
        .ConfigureDefault(CreateDefault)
        .ConfigureBackend(StorageBackendOptions.Use(StorageBackends.Json));

    public static DownloadsSettings CreateDefault(IServiceProvider serviceProvider) => new()
    {
        Folder = new ConfigurablePath(KnownPath.XDG_DATA_HOME, $"{ApplicationConstants.DataDirectoryName}/Downloads"),
    };
}
```

(Si `StorageBackends` no resuelve desde Sdk, usar `StorageBackendOptions.Disable` como `FileExtractorSettings`: la ruta no se edita desde la UI todavía.)

- [ ] **Step 2: Registrar** — en `src/NexusMods.DataModel/Services.cs`, junto a `coll.AddSettings<DataModelSettings>();`: `coll.AddSettings<DownloadsSettings>();`

- [ ] **Step 3: `DataModelSettings`** — borrar la propiedad `DownloadsFolder`, la variable `officialDirectoryName` y su asignación en `CreateDefault`. En `StorageAnalyzer` reemplazar `settings.DownloadsFolder.ToPath(_fileSystem)` por `_settingsManager.Get<DownloadsSettings>().Folder.ToPath(_fileSystem)` (dos lugares).

- [ ] **Step 4: Tests no tocan estado real** — en `AddDefaultServicesForTesting`, junto a los otros `OverrideSettingsForTests`:

```csharp
            .OverrideSettingsForTests<DownloadsSettings>(settings => settings with
            {
                Folder = new ConfigurablePath(baseKnownPath, $"{baseDirectory}/Downloads"),
            })
```

usando las mismas variables `baseKnownPath`/`baseDirectory` que ese método ya usa para otras rutas temporales (si no existen, crearlas como en `tests/NexusMods.Library.Tests/DownloadsService/DIHelpers.cs`: `KnownPath.EntryDirectory` + `$"NexusMods.Tests-{Guid.NewGuid()}"`).

- [ ] **Step 5: Build** → 0/0 (salvo `AddDownloadJob`/`CollectionDownloader`, que siguen con su ruta hardcodeada hasta Task 8; compilan igual).

- [ ] **Step 6: Commit** — `git commit -am "feat: own downloads folder at tModManager/Downloads via DownloadsSettings"`

## Task 7: Atributo `LibraryFile.DownloadPath`

**Files:**
- Modify: `src/NexusMods.Sdk/Library/Models/LibraryFile.cs`
- Modify: `src/NexusMods.Library/AddLibraryFileJob.cs` (`AnalyzeFile`)
- Test: `tests/NexusMods.DataModel.Tests/LibraryServiceTests.cs`

**Interfaces:**
- Produces: `LibraryFile.DownloadPath` (`RelativePathAttribute`, opcional), relativo a `DownloadsSettings.Folder`. Regla única: todo `LibraryFile` de **primer nivel** cuyo archivo de origen está dentro de la carpeta de Descargas lo recibe.

- [ ] **Step 1: Test que falla** (agregar a `LibraryServiceTests`):

```csharp
    [Fact]
    public async Task AddingFileFromDownloadsFolder_RecordsDownloadPath()
    {
        var settings = ServiceProvider.GetRequiredService<ISettingsManager>().Get<DownloadsSettings>();
        var downloads = settings.Folder.ToPath(FileSystem);
        downloads.CreateDirectory();
        var src = FileSystem.GetKnownPath(KnownPath.EntryDirectory).Combine("Resources/data_7zip_lzma2.7z");
        var inDownloads = downloads.Combine("mod-1-0.7z");
        File.Copy(src.ToString(), inDownloads.ToString(), overwrite: true);

        var local = await _libraryService.AddLocalFile(inDownloads);

        local.AsLibraryFile().DownloadPath.Should().Be((RelativePath)"mod-1-0.7z");
    }

    [Fact]
    public async Task AddingFileOutsideDownloadsFolder_HasNoDownloadPath()
    {
        var src = FileSystem.GetKnownPath(KnownPath.EntryDirectory).Combine("Resources/data_7zip_lzma2.7z");
        var local = await _libraryService.AddLocalFile(src);
        LibraryFile.DownloadPath.TryGetValue(local.AsLibraryFile(), out _).Should().BeFalse();
    }
```

(Usar el mismo archivo de recursos que ya usan los tests existentes de esa clase; si el nombre difiere, tomar el de `archivePath` en el test de la línea 42.)

- [ ] **Step 2: Correr** → `RUN tests/NexusMods.DataModel.Tests "FullyQualifiedName~LibraryServiceTests"` → falla: `LibraryFile` no tiene `DownloadPath`.

- [ ] **Step 3: Atributo** — en `LibraryFile.cs`, después de `FileName`:

```csharp
    /// <summary>
    /// Path of the original download, relative to <see cref="DownloadsSettings.Folder"/>.
    /// Only top-level files that live in the downloads folder have it.
    /// </summary>
    public static readonly RelativePathAttribute DownloadPath = new(Namespace, nameof(DownloadPath)) { IsOptional = true };
```

- [ ] **Step 4: Asignarlo en `AddLibraryFileJob`** — agregar la propiedad requerida y resolverla en `Create`:

```csharp
    internal required AbsolutePath DownloadsFolder { get; init; }
```

en `Create`: `DownloadsFolder = provider.GetRequiredService<ISettingsManager>().Get<DownloadsSettings>().Folder.ToPath(provider.GetRequiredService<IFileSystem>()),`

y en `AnalyzeFile`, inmediatamente después de `var libraryFile = CreateLibraryFile(...)`:

```csharp
        if (!isNestedFile && filePath.InFolder(DownloadsFolder))
            Transaction.Add(libraryFile.Id, LibraryFile.DownloadPath, filePath.RelativeTo(DownloadsFolder));
```

- [ ] **Step 5: Correr** → `Passed!`

- [ ] **Step 6: Commit** — `git commit -am "feat: record LibraryFile.DownloadPath for files in the downloads folder"`

## Task 8: Nombre y dedupe en Descargas

**Files:**
- Create: `src/NexusMods.Sdk/Library/DownloadsFolder.cs`
- Modify: `src/NexusMods.Library/AddDownloadJob.cs` (`PreserveOriginalFile`, `StartAsync`, borrar `GetDownloadsFolder`)
- Modify: `src/NexusMods.Collections/CollectionDownloader.cs` (borrar `GetDownloadsFolder`, usar settings)
- Test: `tests/NexusMods.Library.Tests/DownloadsFolderTests.cs`

**Interfaces:**
- Produces: `public static class DownloadsFolder { public static async Task<AbsolutePath> PlaceAsync(AbsolutePath source, AbsolutePath folder, string fileName, CancellationToken ct); }` — devuelve la ruta final; mismo nombre + mismo hash → la existente sin copiar; mismo nombre + otro contenido → sufijo `_1`, `_2`…

- [ ] **Step 1: Tests que fallan** — `tests/NexusMods.Library.Tests/DownloadsFolderTests.cs`:

```csharp
using FluentAssertions;
using NexusMods.Paths;
using NexusMods.Sdk.Library;
using Xunit;

namespace NexusMods.Library.Tests;

public class DownloadsFolderTests : IDisposable
{
    private readonly AbsolutePath _root = FileSystem.Shared.GetKnownPath(KnownPath.TempDirectory).Combine($"dl-{Guid.NewGuid():N}");
    public void Dispose() { if (_root.DirectoryExists()) _root.DeleteDirectory(true); }

    private async Task<AbsolutePath> Temp(string content)
    {
        var p = _root.Combine("tmp").Combine(Guid.NewGuid().ToString("N"));
        p.Parent.CreateDirectory();
        await File.WriteAllTextAsync(p.ToString(), content);
        return p;
    }

    [Fact]
    public async Task Preserve_NewName_CopiesWithNexusName()
    {
        var dest = await DownloadsFolder.PlaceAsync(await Temp("a"), _root.Combine("Downloads"), "Mod-123-1-0.zip", default);
        dest.FileName.ToString().Should().Be("Mod-123-1-0.zip");
        (await File.ReadAllTextAsync(dest.ToString())).Should().Be("a");
    }

    [Fact]
    public async Task Preserve_SameNameSameContent_ReusesExisting()
    {
        var folder = _root.Combine("Downloads");
        var first = await DownloadsFolder.PlaceAsync(await Temp("a"), folder, "Mod.zip", default);
        var second = await DownloadsFolder.PlaceAsync(await Temp("a"), folder, "Mod.zip", default);
        second.Should().Be(first);
        folder.EnumerateFiles("*", recursive: false).Should().ContainSingle();
    }

    [Fact]
    public async Task Preserve_SameNameDifferentContent_GetsSuffix()
    {
        var folder = _root.Combine("Downloads");
        await DownloadsFolder.PlaceAsync(await Temp("v1"), folder, "Mod.zip", default);
        var second = await DownloadsFolder.PlaceAsync(await Temp("v2"), folder, "Mod.zip", default);
        second.FileName.ToString().Should().Be("Mod_1.zip");
        (await File.ReadAllTextAsync(folder.Combine("Mod.zip").ToString())).Should().Be("v1");
    }
}
```

`tests/NexusMods.Library.Tests` no tiene `Startup` global (usa `DIHelpers` por clase); esta clase no inyecta nada.

- [ ] **Step 2: Correr** → `RUN tests/NexusMods.Library.Tests "FullyQualifiedName~DownloadsFolderTests"` → no compila.

- [ ] **Step 3: Implementar** — `src/NexusMods.Sdk/Library/DownloadsFolder.cs`:

```csharp
using NexusMods.Hashing.xxHash3;
using NexusMods.Paths;

namespace NexusMods.Sdk.Library;

/// <summary>
/// Places original downloads in the downloads folder under the name Nexus gives them.
/// </summary>
public static class DownloadsFolder
{
    public static async Task<AbsolutePath> PlaceAsync(AbsolutePath source, AbsolutePath folder, string fileName, CancellationToken ct)
    {
        folder.CreateDirectory();
        var candidate = folder.Combine(fileName);
        var stem = candidate.GetFileNameWithoutExtension();
        var ext = candidate.Extension.ToString();
        Hash? sourceHash = null;

        for (var i = 1; candidate.FileExists; i++)
        {
            sourceHash ??= await HashOf(source, ct);
            if (candidate.FileInfo.Size == source.FileInfo.Size && await HashOf(candidate, ct) == sourceHash)
                return candidate;
            candidate = folder.Combine($"{stem}_{i}{ext}");
        }

        await using (var src = source.Read())
        await using (var dst = candidate.Create())
            await src.CopyToAsync(dst, ct);
        return candidate;
    }

    private static async Task<Hash> HashOf(AbsolutePath path, CancellationToken ct)
    {
        await using var stream = path.Read();
        return await stream.xxHash3Async(token: ct);
    }
}
```

- [ ] **Step 4: `AddDownloadJob`** — `PreserveOriginalFile` pasa a devolver la ruta final (o `null` si falla, logueando como hoy) y el import se hace **desde esa copia**, así `AddLibraryFileJob` asigna `DownloadPath` con la regla de Task 7:

```csharp
        var preserved = await PreserveOriginalFile(context.CancellationToken);

        await context.YieldAsync();
        using var tx = Connection.BeginTransaction();

        var libraryFile = await AddLibraryFileJob.Create(ServiceProvider, tx, preserved ?? DownloadJob.Result);
```

```csharp
    private async Task<AbsolutePath?> PreserveOriginalFile(CancellationToken ct)
    {
        try
        {
            var downloaded = DownloadJob.Result;
            if (!downloaded.FileExists) return null;

            var settings = ServiceProvider.GetRequiredService<ISettingsManager>().Get<DownloadsSettings>();
            var folder = settings.Folder.ToPath(ServiceProvider.GetRequiredService<IFileSystem>());
            var name = await GetMeaningfulFileNameAsync(downloaded, ct);
            return await DownloadsFolder.PlaceAsync(downloaded, folder, name, ct);
        }
        catch (Exception ex)
        {
            ServiceProvider.GetService<ILogger<AddDownloadJob>>()?.LogWarning(ex, "No se pudo preservar el archivo original descargado");
            return null;
        }
    }
```

Borrar `GetDownloadsFolder` de `AddDownloadJob`.

- [ ] **Step 5: `CollectionDownloader`** — borrar `GetDownloadsFolder` (línea ~919); en `RescanDownloads` reemplazar `var downloadsFolder = GetDownloadsFolder(fs);` por:

```csharp
        var downloadsFolder = _serviceProvider.GetRequiredService<ISettingsManager>().Get<DownloadsSettings>().Folder.ToPath(fs);
```

Como los archivos del rescan ya están en la carpeta, `AddLocalFile` les asigna `DownloadPath` solo (Task 7).

- [ ] **Step 6: Correr** → `RUN tests/NexusMods.Library.Tests "FullyQualifiedName~DownloadsFolderTests"` → `Passed! 3`; build 0/0.

- [ ] **Step 7: Commit** — `git commit -am "feat: keep downloads under the Nexus file name, dedupe by content, single folder setting"`

## Task 9: `IDownloadReExtractor`

**Files:**
- Create: `src/NexusMods.Sdk/FileStore/IDownloadReExtractor.cs`
- Create: `src/NexusMods.Library/DownloadReExtractor.cs`
- Modify: `src/NexusMods.Library/Services.cs` (registro)
- Test: `tests/NexusMods.DataModel.Tests/DownloadReExtractorTests.cs`

**Interfaces:**
- Produces: `public interface IDownloadReExtractor { Task<IReadOnlySet<Hash>> RestoreAsync(IReadOnlyCollection<Hash> missing, CancellationToken ct); }` — devuelve los hashes que quedaron repuestos. Interfaz en Sdk porque el consumidor (`ALoadoutSynchronizer`, en `Abstractions.Loadouts.Synchronizers`) no referencia `NexusMods.Library`.

- [ ] **Step 1: Test que falla** — `tests/NexusMods.DataModel.Tests/DownloadReExtractorTests.cs`:

```csharp
using FluentAssertions;
using Microsoft.Extensions.DependencyInjection;
using NexusMods.Abstractions.Library;
using NexusMods.Games.TestFramework;
using NexusMods.Paths;
using NexusMods.Sdk.FileStore;
using NexusMods.Sdk.Library;
using NexusMods.Sdk.Settings;
using Xunit;

namespace NexusMods.DataModel.Tests;

public class DownloadReExtractorTests(ITestOutputHelper helper) : ACyberpunkIsolatedGameTest<DownloadReExtractorTests>(helper)
{
    [Fact]
    public async Task RestoresEntriesFromTheOriginalDownload()
    {
        var library = ServiceProvider.GetRequiredService<ILibraryService>();
        var store = (LooseFileStore)ServiceProvider.GetRequiredService<IFileStore>();
        var reExtractor = ServiceProvider.GetRequiredService<IDownloadReExtractor>();
        var downloads = ServiceProvider.GetRequiredService<ISettingsManager>().Get<DownloadsSettings>().Folder.ToPath(FileSystem);
        downloads.CreateDirectory();
        var src = FileSystem.GetKnownPath(KnownPath.EntryDirectory).Combine("Resources/data_7zip_lzma2.7z");
        var inDownloads = downloads.Combine("mod.7z");
        File.Copy(src.ToString(), inDownloads.ToString(), overwrite: true);

        var local = await library.AddLocalFile(inDownloads);
        var entryHashes = LibraryArchiveFileEntry.FindByParent(Connection.Db, local.AsLibraryFile().Id)
            .Select(e => e.AsLibraryFile().Hash).ToArray();
        foreach (var h in entryHashes) store.PathFor(h).Delete();

        var restored = await reExtractor.RestoreAsync(entryHashes, default);

        restored.Should().BeEquivalentTo(entryHashes);
        foreach (var h in entryHashes) (await store.HaveFile(h)).Should().BeTrue();
    }

    [Fact]
    public async Task WithoutDownload_RestoresNothing()
    {
        var reExtractor = ServiceProvider.GetRequiredService<IDownloadReExtractor>();
        var restored = await reExtractor.RestoreAsync([NexusMods.Hashing.xxHash3.Hash.From(123)], default);
        restored.Should().BeEmpty();
    }
}
```

(Mismo archivo de recursos que Task 7; `LibraryArchiveFileEntry.FindByParent` ya se usa en `FileStoreTests`.)

- [ ] **Step 2: Correr** → no compila.

- [ ] **Step 3: Interfaz** — `src/NexusMods.Sdk/FileStore/IDownloadReExtractor.cs`:

```csharp
using NexusMods.Hashing.xxHash3;

namespace NexusMods.Sdk.FileStore;

/// <summary>
/// Puts missing files back into the <see cref="IFileStore"/> by extracting them again from the
/// original downloads they came from.
/// </summary>
public interface IDownloadReExtractor
{
    /// <summary>Returns the hashes that are in the store after the call.</summary>
    Task<IReadOnlySet<Hash>> RestoreAsync(IReadOnlyCollection<Hash> missing, CancellationToken ct);
}
```

- [ ] **Step 4: Implementación** — `src/NexusMods.Library/DownloadReExtractor.cs`:

```csharp
using Microsoft.Extensions.Logging;
using NexusMods.Hashing.xxHash3;
using NexusMods.MnemonicDB.Abstractions;
using NexusMods.Paths;
using NexusMods.Sdk.FileExtractor;
using NexusMods.Sdk.FileStore;
using NexusMods.Sdk.IO;
using NexusMods.Sdk.Library;
using NexusMods.Sdk.Settings;

namespace NexusMods.Library;

internal sealed class DownloadReExtractor(
    IConnection connection,
    IFileStore store,
    IFileExtractor extractor,
    TemporaryFileManager temp,
    ISettingsManager settings,
    IFileSystem fs,
    ILogger<DownloadReExtractor> logger) : IDownloadReExtractor
{
    public async Task<IReadOnlySet<Hash>> RestoreAsync(IReadOnlyCollection<Hash> missing, CancellationToken ct)
    {
        var restored = new HashSet<Hash>();
        if (missing.Count == 0) return restored;
        var db = connection.Db;
        var downloads = settings.Get<DownloadsSettings>().Folder.ToPath(fs);

        // Group the wanted hashes by the top-level download that contains them.
        var byDownload = new Dictionary<AbsolutePath, List<(RelativePath[] Chain, Hash Hash)>>();
        foreach (var hash in missing.Distinct())
        {
            foreach (var file in LibraryFile.FindByHash(db, hash))
            {
                if (!TryChain(db, file, out var top, out var chain)) continue;
                if (!LibraryFile.DownloadPath.TryGetValue(top, out var rel)) continue;
                var archive = downloads.Combine(rel);
                if (!archive.FileExists) continue;
                if (!byDownload.TryGetValue(archive, out var list)) byDownload[archive] = list = [];
                list.Add((chain, hash));
                break;
            }
        }

        foreach (var (archive, wanted) in byDownload)
        {
            try
            {
                await using var dir = temp.CreateFolder();
                await extractor.ExtractAllAsync(archive, dir.Path, token: ct);
                var toBackup = new List<ArchivedFileEntry>();
                foreach (var (chain, hash) in wanted)
                {
                    var path = await Resolve(dir.Path, chain, ct);
                    if (path is null || !path.Value.FileExists) continue;
                    toBackup.Add(new ArchivedFileEntry(new NativeFileStreamFactory(path.Value), hash, path.Value.FileInfo.Size));
                }
                await store.BackupFiles(toBackup, token: ct);
                foreach (var e in toBackup) restored.Add(e.Hash);
                logger.LogInformation("Reextraídos {Count} archivos desde {Archive}", toBackup.Count, archive.FileName);
            }
            catch (Exception ex)
            {
                logger.LogWarning(ex, "No se pudo reextraer desde {Archive}", archive);
            }
        }
        return restored;
    }

    /// <summary>
    /// Walks up LibraryArchiveFileEntry parents to the top-level file. <paramref name="chain"/> holds the
    /// entry paths from the top-level archive down (more than one element means nested archives).
    /// </summary>
    private static bool TryChain(IDb db, LibraryFile.ReadOnly file, out LibraryFile.ReadOnly top, out RelativePath[] chain)
    {
        var parts = new List<RelativePath>();
        var current = file;
        while (LibraryArchiveFileEntry.Path.TryGetValue(current, out var path))
        {
            parts.Add(path);
            var entry = new LibraryArchiveFileEntry.ReadOnly(db, current.Id);
            current = entry.Parent.AsLibraryFile();
        }
        parts.Reverse();
        top = current;
        chain = parts.ToArray();
        return parts.Count > 0;
    }

    private async Task<AbsolutePath?> Resolve(AbsolutePath root, RelativePath[] chain, CancellationToken ct)
    {
        var path = root.Combine(chain[0]);
        for (var i = 1; i < chain.Length; i++)
        {
            if (!path.FileExists) return null;
            var nested = temp.CreateFolder();
            await extractor.ExtractAllAsync(path, nested.Path, token: ct);
            path = nested.Path.Combine(chain[i]);
        }
        return path;
    }
}
```

Nota para el implementador: confirmar en `LibraryArchiveFileEntry.cs` y `LibraryArchive.cs` (`src/NexusMods.Sdk/Library/Models/`) los nombres exactos de `Parent` y `AsLibraryFile()`; el código de `AddLibraryFileJob.AnalyzeFile` (`ParentId = libraryArchive.Id`) muestra que el padre es un `LibraryArchive` que incluye `LibraryFile`. Las carpetas temporales de los anidados se limpian con el `TemporaryFileManager` al cerrar la app (ponytail: no se disponen una por una).

- [ ] **Step 5: Registro** — en `src/NexusMods.Library/Services.cs`: `.AddSingleton<IDownloadReExtractor, DownloadReExtractor>()`.

- [ ] **Step 6: Correr** → `RUN tests/NexusMods.DataModel.Tests "FullyQualifiedName~DownloadReExtractorTests"` → `Passed! 2`.

- [ ] **Step 7: Commit** — `git commit -am "feat: re-extract missing store files from their original downloads"`

## Task 10: Usar la reextracción en Apply y colecciones

**Files:**
- Modify: `src/NexusMods.Abstractions.Loadouts.Synchronizers/ALoadoutSynchronizer.cs` (ctor + `ActionExtractToDisk`)
- Modify: `src/NexusMods.Collections/InstallCollectionDownloadJob.cs` (`TryReExtractMissingFiles`)
- Modify: `src/NexusMods.Collections/InstallCollectionJob.cs:100-106`
- Test: `tests/NexusMods.DataModel.Synchronizer.Tests/` (nuevo caso)

**Interfaces:**
- Consumes: `IDownloadReExtractor.RestoreAsync` (Task 9), `LibraryFile.DownloadPath` (Task 7), `DownloadsSettings` (Task 6).

- [ ] **Step 1: Test que falla** — en `tests/NexusMods.DataModel.Synchronizer.Tests/`, archivo nuevo `ReExtractOnApplyTests.cs`, siguiendo el patrón de `GeneralModManagementTests` (misma clase base y `Startup` del proyecto): agregar un mod desde un archivo copiado a la carpeta de Descargas, instalarlo, borrar el contenido del store (`((LooseFileStore)FileStore).PathFor(h).Delete()` para todos los hashes del mod), sincronizar el loadout y afirmar que los archivos del mod existen en la carpeta del juego.

```csharp
    [Fact]
    public async Task Apply_AfterStoreWiped_ReExtractsFromDownloads()
    {
        var downloads = ServiceProvider.GetRequiredService<ISettingsManager>().Get<DownloadsSettings>().Folder.ToPath(FileSystem);
        downloads.CreateDirectory();
        var zip = downloads.Combine("mod.zip");
        await using (var fs = zip.Create())
        using (var archive = new System.IO.Compression.ZipArchive(fs, System.IO.Compression.ZipArchiveMode.Create))
        {
            await using var w = new StreamWriter(archive.CreateEntry("archive/pc/mod/test.archive").Open());
            await w.WriteAsync("contenido de prueba");
        }

        var loadout = await CreateLoadout();
        var local = await LibraryService.AddLocalFile(zip);
        await LibraryService.InstallItem(local.AsLibraryFile().AsLibraryItem(), loadout.LoadoutId);
        var hashes = LibraryArchiveFileEntry.FindByParent(Connection.Db, local.AsLibraryFile().Id).Select(e => e.AsLibraryFile().Hash);
        foreach (var h in hashes) ((LooseFileStore)FileStore).PathFor(h).Delete();

        await Synchronizer.Synchronize(loadout);

        GameInstallation.LocationsRegister[LocationId.Game].Combine("archive/pc/mod/test.archive").FileExists.Should().BeTrue();
    }
```

Nota: ajustar los nombres (`LibraryService`, `Synchronizer`, `InstallItem`, `GameInstallation.LocationsRegister`) a los que expone la clase base de ese proyecto; usar como referencia un test existente del mismo archivo que instale un mod y sincronice.

- [ ] **Step 2: Correr** → falla con `MissingArchiveException`.

- [ ] **Step 3: `ALoadoutSynchronizer`** — agregar el campo `private readonly IDownloadReExtractor _reExtractor;`, parámetro en el ctor protegido completo y `provider.GetRequiredService<IDownloadReExtractor>()` en el ctor por `IServiceProvider` (mismo patrón que `IFileStore` en las líneas 71-105). En `ActionExtractToDisk`, antes de `await _fileStore.ExtractFiles(...)`:

```csharp
            var missing = toExtract.Select(x => x.Hash).Where(h => !_fileStore.HaveFile(h).Result).ToArray();
            if (missing.Length > 0)
            {
                var restored = await _reExtractor.RestoreAsync(missing, CancellationToken.None);
                Logger.LogInformation("Faltaban {Missing} archivos en el store; {Restored} reextraídos desde Descargas", missing.Length, restored.Count);
            }
```

`HaveFile` es sincrónico en `LooseFileStore` (`ValueTask.FromResult`), así que `.Result` no bloquea (mismo uso que `HaveArchive` en la línea 1065). Lo que siga faltando termina en `MissingArchiveException` desde `ExtractFiles`, como hoy.

Si alguna subclase de `ALoadoutSynchronizer` llama al ctor completo (buscar `: base(` en `src/**/*Synchronizer.cs`), pasarle el servicio nuevo.

- [ ] **Step 4: Colecciones** — en `InstallCollectionDownloadJob.TryReExtractMissingFiles` reemplazar el cuerpo del `try` completo (desde `var fileExtractor` hasta `return recovered;`) por:

```csharp
        var reExtractor = ServiceProvider.GetRequiredService<IDownloadReExtractor>();
        var wanted = failedChildren.Select(c => c.AsLibraryFile().Hash).ToArray();
        var restored = await reExtractor.RestoreAsync(wanted, default);
        var recovered = 0;
        foreach (var child in failedChildren)
        {
            var hash = child.AsLibraryFile().Hash;
            if (!restored.Contains(hash)) continue;
            await using var stream = await FileStore.GetFileStream(hash);
            var md5 = await Md5Hasher.HashAsync(stream);
            var mapping = new HashMapping { Hash = hash, Size = child.AsLibraryFile().Size };
            hashes[md5] = mapping;
            pathIndex[child.Path] = mapping;
            recovered++;
        }
        return recovered;
```

y borrar el chequeo inicial `if (!await FileStore.HaveFile(parentLibraryFile.Hash)) { ... return 0; }` (esa era la razón de que nunca funcionara). El parámetro `parentLibraryFile` queda sin uso: sacarlo de la firma y del llamador (línea ~367).

- [ ] **Step 5: Paquete de colección** — en `InstallCollectionJob.cs:~104` reemplazar `!await FileStore.HaveFile(collectionFileHash)` por un chequeo contra Descargas:

```csharp
        var downloads = ServiceProvider.GetRequiredService<ISettingsManager>().Get<DownloadsSettings>().Folder.ToPath(ServiceProvider.GetRequiredService<IFileSystem>());
        var packageOnDisk = SourceCollection.IsValid()
            && LibraryFile.DownloadPath.TryGetValue(SourceCollection.AsLibraryFile(), out var packageRel)
            && downloads.Combine(packageRel).FileExists;
        if (!SourceCollection.IsValid() || !packageOnDisk)
```

y actualizar el comentario anterior ("the NX archive gets deleted" → "the downloaded package was deleted"). La variable `collectionFileHash` queda sin uso: borrarla.

- [ ] **Step 6: Correr** → `RUN tests/NexusMods.DataModel.Synchronizer.Tests "FullyQualifiedName~ReExtractOnApplyTests"` → `Passed!`; `RUN tests/NexusMods.Collections.Tests` → sin fallos nuevos respecto de `$S/ci-main.log`.

- [ ] **Step 7: Commit** — `git commit -am "feat: re-extract from downloads on apply and collection install; stop re-downloading collection packages"`

## Task 11: Deep Clean deja Descargas en paz

**Files:**
- Modify: `src/NexusMods.DataModel/Storage/IStorageAnalyzer.cs`, `StorageAnalyzer.cs`
- Modify: `src/NexusMods.App.UI/Pages/StorageManager/StorageManagerPageViewModel.cs` (+ `IStorageManagerPageViewModel` y la `.axaml` de la página)

**Interfaces:**
- Produces: `IStorageAnalyzer.DeleteDownloadsAsync(CancellationToken)`; `DeletePhysicalFilesAsync` solo borra backups de Deep Clean.

- [ ] **Step 1: Separar** — en `StorageAnalyzer.DeletePhysicalFilesAsync` borrar el bloque de `downloadsPath`; agregar:

```csharp
    public Task DeleteDownloadsAsync(CancellationToken cancellationToken = default)
    {
        var downloads = _settingsManager.Get<DownloadsSettings>().Folder.ToPath(_fileSystem);
        if (downloads.DirectoryExists())
            foreach (var file in downloads.EnumerateFiles("*", recursive: false))
                file.Delete();
        return Task.CompletedTask;
    }
```

y declararlo en `IStorageAnalyzer` con doc comment "Borra las descargas originales. Solo por acción explícita del usuario."

- [ ] **Step 2: Botón** — en `StorageManagerPageViewModel` agregar `public ReactiveCommand<Unit> DeleteDownloadsCommand { get; }` (declararlo en la interfaz del VM), con el mismo patrón que `DeepCleanCommand`: diálogo `DialogFactory.CreateDialog(title: "Borrar descargas", ...)` con botones "Cancelar" / "Borrar" (`ButtonStyling.Destructive`) y un `contentViewModel` de texto: "Se borran los archivos originales de tModManager/Downloads. Vas a tener que volver a bajarlos para reinstalar o reconstruir el store." Al aceptar: `await storageAnalyzer.DeleteDownloadsAsync(ct); await RefreshStatsAsync(storageAnalyzer, ct);` bajo `IsBusy`. En la vista, un botón junto al de Deep Clean enlazado a ese comando (copiar el `Button` de Deep Clean y cambiar `Command` y texto).

- [ ] **Step 3: Build** → 0/0. `RUN tests/NexusMods.UI.Tests` → sin fallos nuevos.

- [ ] **Step 4: Suite completa vs `main`** (igual que Task 5 Step 6, log `$S/ci-pr2.log`) → sin fallos nuevos.

- [ ] **Step 5: Commit + PR 2**

```bash
git commit -am "feat: Deep Clean keeps downloads; explicit delete-downloads action in Storage Manager"
git push -u origin feat/downloads-first-class
gh pr create --base feat/remove-nx --title "feat: first-class downloads in tModManager/Downloads" --body "Descargas en tModManager/Downloads (DownloadsSettings) con el nombre de Nexus y dedupe por contenido; LibraryFile.DownloadPath; IDownloadReExtractor repone el store en Apply y colecciones (arregla la reextracción que nunca funcionaba y el re-download del paquete de colección); Deep Clean ya no borra Descargas, botón aparte. PR 2 de 3."
```

---

# PR 3 — Asistente de datos viejos, Deep Clean reforzado, prefix de Proton

Rama: `git checkout -b feat/legacy-cleanup-wizard` desde `feat/downloads-first-class`. PR con base `feat/downloads-first-class`.

## Task 12: Detector de datos viejos

**Files:**
- Create: `src/NexusMods.DataModel/LegacyData/LegacyDataDetector.cs`
- Test: `tests/NexusMods.DataModel.Tests/LegacyDataDetectorTests.cs`

**Interfaces:**
- Produces: `public static class LegacyDataDetector { public static bool HasNxArchives(AbsolutePath archivesRoot); public static AbsolutePath LegacyDownloadsFolder(IFileSystem fs); public static AbsolutePath LegacyBackupsFolder(IFileSystem fs); public static void RequestResetOnStart(IFileSystem fs); public static bool ResetIfRequested(AbsolutePath archivesRoot, AbsolutePath mnemonicDbPath, IFileSystem fs); }`

- [ ] **Step 1: Tests que fallan**

```csharp
using FluentAssertions;
using NexusMods.DataModel.LegacyData;
using NexusMods.Paths;
using Xunit;

namespace NexusMods.DataModel.Tests;

public class LegacyDataDetectorTests : IDisposable
{
    private readonly AbsolutePath _root = FileSystem.Shared.GetKnownPath(KnownPath.TempDirectory).Combine($"legacy-{Guid.NewGuid():N}");
    public void Dispose() { if (_root.DirectoryExists()) _root.DeleteDirectory(true); }

    [Fact]
    public void Detector_WithNx_ReturnsTrue()
    {
        _root.CreateDirectory();
        _root.Combine("3f2a.nx").Create().Dispose();
        LegacyDataDetector.HasNxArchives(_root).Should().BeTrue();
    }

    [Fact]
    public void Detector_NoNx_ReturnsFalse_EvenWithLegacyDownloads()
    {
        _root.Combine("ab").CreateDirectory();
        _root.Combine("ab/abcdef0123456789").Create().Dispose();
        LegacyDataDetector.HasNxArchives(_root).Should().BeFalse();
    }

    [Fact]
    public void Detector_MissingFolder_ReturnsFalse() =>
        LegacyDataDetector.HasNxArchives(_root.Combine("nope")).Should().BeFalse();

    [Fact]
    public void ResetIfRequested_WithoutMarker_DoesNothing()
    {
        _root.CreateDirectory();
        _root.Combine("a.nx").Create().Dispose();
        var fs = new InMemoryFileSystem();
        LegacyDataDetector.ResetIfRequested(_root, _root.Combine("db"), fs).Should().BeFalse();
        _root.Combine("a.nx").FileExists.Should().BeTrue();
    }
}
```

- [ ] **Step 2: Correr** → no compila.

- [ ] **Step 3: Implementar**

```csharp
using NexusMods.Paths;

namespace NexusMods.DataModel.LegacyData;

/// <summary>
/// Finds data left by versions that stored mods in the Nexus <c>.nx</c> format.
/// </summary>
public static class LegacyDataDetector
{
    public static bool HasNxArchives(AbsolutePath archivesRoot) =>
        archivesRoot.DirectoryExists() && archivesRoot.EnumerateFiles("*.nx", recursive: false).Any();

    /// <summary>Downloads folder shared with the official NexusMods.App.</summary>
    public static AbsolutePath LegacyDownloadsFolder(IFileSystem fs) =>
        fs.GetKnownPath(KnownPath.XDG_DATA_HOME).Combine("NexusMods.App").Combine("Downloads");

    /// <summary>Where Deep Clean used to put its backups.</summary>
    public static AbsolutePath LegacyBackupsFolder(IFileSystem fs) =>
        fs.GetKnownPath(KnownPath.XDG_DATA_HOME).Combine("NexusMods.App").Combine("CyberpunkBackups");

    private static AbsolutePath ResetMarker(IFileSystem fs) =>
        fs.GetKnownPath(KnownPath.XDG_DATA_HOME).Combine(NexusMods.Sdk.ApplicationConstants.DataDirectoryName).Combine("reset-on-start");

    /// <summary>
    /// The database cannot be deleted while it is open, so the wizard leaves a marker and restarts;
    /// <see cref="ResetIfRequested"/> runs before the connection opens.
    /// </summary>
    public static void RequestResetOnStart(IFileSystem fs)
    {
        var marker = ResetMarker(fs);
        marker.Parent.CreateDirectory();
        marker.Create().Dispose();
    }

    /// <summary>Deletes the <c>.nx</c> archives and the database if a reset was requested. Returns true if it did.</summary>
    public static bool ResetIfRequested(AbsolutePath archivesRoot, AbsolutePath mnemonicDbPath, IFileSystem fs)
    {
        var marker = ResetMarker(fs);
        if (!marker.FileExists) return false;
        if (archivesRoot.DirectoryExists())
            foreach (var nx in archivesRoot.EnumerateFiles("*.nx", recursive: false)) nx.Delete();
        if (mnemonicDbPath.DirectoryExists()) mnemonicDbPath.DeleteDirectory(recursive: true);
        marker.Delete();
        return true;
    }
}
```

- [ ] **Step 4: Correr** → `Passed! 3`.

- [ ] **Step 5: Commit** — `git commit -am "feat: detect leftover .nx data"`

## Task 13: Deep Clean reforzado y backups en `tModManager/Backups`

**Files:**
- Modify: `src/NexusMods.Games.RedEngine/Cyberpunk2077/CyberpunkDeepCleanTool.cs`
- Modify: `src/NexusMods.DataModel/Storage/StorageAnalyzer.cs` (`GetCyberpunkBackupsPath`)
- Test: `tests/Games/NexusMods.Games.RedEngine.Tests/CyberpunkDeepCleanToolTests.cs`

**Interfaces:**
- Produces: `CyberpunkDeepCleanTool.BackupsRoot(IFileSystem)` (público, estático) = `XDG_DATA_HOME/tModManager/Backups`; `internal static IReadOnlyList<RelativePath> FindLooseModFiles(AbsolutePath gameRoot, IReadOnlySet<GamePath> vanilla)`.

- [ ] **Step 1: Tests que fallan** — probar la parte pura (qué se mueve) sin DI:

```csharp
using FluentAssertions;
using NexusMods.Games.RedEngine.Cyberpunk2077;
using NexusMods.Paths;
using NexusMods.Sdk.Games;
using Xunit;

namespace NexusMods.Games.RedEngine.Tests;

public class CyberpunkDeepCleanToolTests : IDisposable
{
    private readonly AbsolutePath _game = FileSystem.Shared.GetKnownPath(KnownPath.TempDirectory).Combine($"cp-{Guid.NewGuid():N}");
    public void Dispose() { if (_game.DirectoryExists()) _game.DeleteDirectory(true); }

    private void Touch(string rel) { var p = _game.Combine(rel); p.Parent.CreateDirectory(); p.Create().Dispose(); }

    [Fact]
    public void LooseFiles_NonVanillaRootAndConfigFiles_AreFound_VanillaKept()
    {
        Touch("FlatlinedExit_readme.txt");
        Touch("REDprelauncher.exe");
        Touch("engine/config/platform/pc/input_loader.ini");
        Touch("engine/config/platform/pc/user.ini");     // vanilla in this test
        Touch("r6/cache/final.redscripts.bk");
        Touch("tools/redmod/tweaks/base/gameplay/static_data/database/devices.tweak");
        Touch("bin/x64/CyberPunk.bat");
        var vanilla = new HashSet<GamePath>
        {
            new(LocationId.Game, "engine/config/platform/pc/user.ini"),
            new(LocationId.Game, "REDprelauncher.exe"),
        };

        var found = CyberpunkDeepCleanTool.FindLooseModFiles(_game, vanilla).Select(p => p.ToString()).ToArray();

        found.Should().BeEquivalentTo(
            "FlatlinedExit_readme.txt",
            "engine/config/platform/pc/input_loader.ini",
            "r6/cache/final.redscripts.bk",
            "tools/redmod/tweaks/base/gameplay/static_data/database/devices.tweak",
            "bin/x64/CyberPunk.bat");
    }
}
```

(`REDprelauncher.exe` figura como vanilla en este test para comprobar que la lista vanilla manda; en una instalación real sin ese archivo en la lista, se movería.)

- [ ] **Step 2: Correr** → `RUN tests/Games/NexusMods.Games.RedEngine.Tests "FullyQualifiedName~CyberpunkDeepCleanToolTests"` → no compila.

- [ ] **Step 3: Implementar** — en `CyberpunkDeepCleanTool`:

1. Agregar a `PathsToMove` (directorios que solo crean mods):

```csharp
        "r6/audioware",
        "r6/input",
        "r6/config/cybercmd",
        "r6/config/redsUserHints",
        "r6/publishing",
        "r6/logs",
```

2. Agregar la búsqueda de archivos sueltos no vanilla:

```csharp
    // Files mods drop outside their own folders. Moved only when the game's file list says they are not vanilla.
    private static readonly string[] LooseFileGlobs =
    [
        "*",                                   // game root, top level only
        "engine/config/platform/pc/*.ini",
        "engine/config/base/scripts.ini",
        "r6/cache/final.redscripts*",
        "r6/cache/input*.xml",
        "tools/redmod/tweaks/**/devices.tweak",
        "bin/x64/CyberPunk.bat",
    ];

    internal static IReadOnlyList<RelativePath> FindLooseModFiles(AbsolutePath gameRoot, IReadOnlySet<GamePath> vanilla)
    {
        var matcher = new Microsoft.Extensions.FileSystemGlobbing.Matcher();
        matcher.AddIncludePatterns(LooseFileGlobs);
        var result = matcher.Execute(new Microsoft.Extensions.FileSystemGlobbing.Abstractions.DirectoryInfoWrapper(new DirectoryInfo(gameRoot.ToString())));
        return result.Files
            .Select(f => RelativePath.FromUnsanitizedInput(f.Path))
            .Where(rel => !vanilla.Contains(new GamePath(LocationId.Game, rel)))
            .OrderBy(rel => rel.ToString(), StringComparer.Ordinal)
            .ToArray();
    }
```

(`Microsoft.Extensions.FileSystemGlobbing` ya está en `Directory.Packages.props`; agregar su `PackageReference` sin versión a `NexusMods.Games.RedEngine.csproj` si no lo tiene. Nota: el glob `"*"` matchea solo el primer nivel.)

3. En `Execute`, después del loop de `PathsToMove`, mover también los archivos sueltos. La lista vanilla sale del servicio de hashes (inyectar `IFileHashesService` en el ctor):

```csharp
        var vanilla = _fileHashes.GetGameFiles((loadout.Installation.Store, loadout.LocatorIds.ToArray()))
            .Select(f => f.Path).ToHashSet();
        foreach (var rel in FindLooseModFiles(gamePath, vanilla))
            MoveToBackup(gamePath.Combine(rel), backupDir.Combine(rel), ref backupCreated, backupDir);
```

extrayendo el cuerpo de movimiento del loop existente a un método privado `MoveToBackup(AbsolutePath from, AbsolutePath to, ref bool backupCreated, AbsolutePath backupDir)` usado por ambos loops.

4. `backupsRoot` pasa a `BackupsRoot(_fileSystem)`:

```csharp
    public static AbsolutePath BackupsRoot(IFileSystem fs) =>
        fs.GetKnownPath(KnownPath.XDG_DATA_HOME).Combine(ApplicationConstants.DataDirectoryName).Combine("Backups");
```

5. Borrar el comentario sobre "we have no Steam" (ahora sí se ofrece verificar en Steam desde el asistente).

- [ ] **Step 4: `StorageAnalyzer.GetCyberpunkBackupsPath`** → `CyberpunkDeepCleanTool.BackupsRoot(_fileSystem)` si `DataModel` referencia `Games.RedEngine`; si no (lo esperable), duplicar la expresión con un comentario `// keep in sync with CyberpunkDeepCleanTool.BackupsRoot`. **No** agregar una referencia de DataModel a un juego.

- [ ] **Step 5: Correr** → `Passed!`; `RUN tests/Games/NexusMods.Games.RedEngine.Tests` → sin fallos nuevos.

- [ ] **Step 6: Commit** — `git commit -am "feat: stronger Deep Clean (mod leftovers outside mod folders), backups under tModManager/Backups"`

## Task 14: Acciones del asistente en `IStorageAnalyzer`

**Files:**
- Modify: `src/NexusMods.DataModel/Storage/IStorageAnalyzer.cs`, `StorageAnalyzer.cs`
- Test: `tests/NexusMods.DataModel.Tests/LegacyDataDetectorTests.cs` (casos de mover descargas)

**Interfaces:**
- Produces:
  - `Task<(int Count, Size Size)> GetLegacyDownloadsAsync(CancellationToken)`
  - `Task<int> MoveLegacyDownloadsAsync(CancellationToken)` — mueve `NexusMods.App/Downloads/*` a `DownloadsSettings.Folder` con la regla de `DownloadsFolder.PlaceAsync` semántica (mismo nombre+hash → descarta origen; distinto → sufijo).
  - `Task DeleteProtonPrefixAsync(AbsolutePath steamLibraryRoot, CancellationToken)` — borra `steamapps/compatdata/1091500`.
  - El borrado de la base y los `.nx` **no** vive acá: lo hace `LegacyDataDetector.ResetIfRequested` al arrancar (Task 12), porque la base está abierta mientras corre el asistente.

- [ ] **Step 1: Test de mover descargas** — `StorageAnalyzer` ya tiene DI completo en `DataModel.Tests`; agregar a `LegacyDataDetectorTests` una clase aparte `LegacyDownloadsMoveTests : ACyberpunkIsolatedGameTest<LegacyDownloadsMoveTests>` que:
  1. cree dos archivos en `LegacyDataDetector.LegacyDownloadsFolder(FileSystem)` **redirigido a temp**: para no tocar el `~/.local/share` real, `MoveLegacyDownloadsAsync` toma la carpeta origen de una propiedad `internal Func<AbsolutePath> LegacyDownloadsFolderProvider` que el test sobreescribe;
  2. llame `MoveLegacyDownloadsAsync` y afirme que el origen quedó vacío y el destino tiene ambos archivos con su nombre.

```csharp
public class LegacyDownloadsMoveTests(ITestOutputHelper helper) : ACyberpunkIsolatedGameTest<LegacyDownloadsMoveTests>(helper)
{
    [Fact]
    public async Task MoveLegacyDownloads_MovesAndKeepsNames()
    {
        var analyzer = (StorageAnalyzer)ServiceProvider.GetRequiredService<IStorageAnalyzer>();
        var legacy = TemporaryFileManager.CreateFolder().Path;
        await File.WriteAllTextAsync(legacy.Combine("A-1-0.zip").ToString(), "a");
        await File.WriteAllTextAsync(legacy.Combine("B-2-0.7z").ToString(), "b");
        analyzer.LegacyDownloadsFolderProvider = () => legacy;

        var moved = await analyzer.MoveLegacyDownloadsAsync(default);

        moved.Should().Be(2);
        legacy.EnumerateFiles("*", recursive: false).Should().BeEmpty();
        var dest = ServiceProvider.GetRequiredService<ISettingsManager>().Get<DownloadsSettings>().Folder.ToPath(FileSystem);
        dest.Combine("A-1-0.zip").FileExists.Should().BeTrue();
        dest.Combine("B-2-0.7z").FileExists.Should().BeTrue();
    }
}
```

- [ ] **Step 2: Correr** → no compila.

- [ ] **Step 3: Implementar** en `StorageAnalyzer` (y declarar en la interfaz):

```csharp
    internal Func<AbsolutePath> LegacyDownloadsFolderProvider { get; set; }
    // en el ctor: LegacyDownloadsFolderProvider = () => LegacyDataDetector.LegacyDownloadsFolder(fileSystem);

    public Task<(int Count, Size Size)> GetLegacyDownloadsAsync(CancellationToken cancellationToken = default)
    {
        var folder = LegacyDownloadsFolderProvider();
        if (!folder.DirectoryExists()) return Task.FromResult((0, Size.Zero));
        var files = folder.EnumerateFiles("*", recursive: false).ToArray();
        return Task.FromResult((files.Length, Size.From(files.Aggregate(0UL, (acc, f) => acc + f.FileInfo.Size.Value))));
    }

    public async Task<int> MoveLegacyDownloadsAsync(CancellationToken cancellationToken = default)
    {
        var from = LegacyDownloadsFolderProvider();
        if (!from.DirectoryExists()) return 0;
        var to = _settingsManager.Get<DownloadsSettings>().Folder.ToPath(_fileSystem);
        var moved = 0;
        foreach (var file in from.EnumerateFiles("*", recursive: false).ToArray())
        {
            await DownloadsFolder.PlaceAsync(file, to, file.FileName.ToString(), cancellationToken);
            file.Delete();
            moved++;
        }
        return moved;
    }

    public Task DeleteProtonPrefixAsync(AbsolutePath steamLibraryRoot, CancellationToken cancellationToken = default)
    {
        var prefix = steamLibraryRoot.Combine("steamapps/compatdata/1091500");
        if (prefix.DirectoryExists()) prefix.DeleteDirectory(recursive: true);
        return Task.CompletedTask;
    }
```

**Reset al arrancar**: en `src/NexusMods.DataModel/Services.cs`, en el punto donde se construye la conexión MnemonicDB a partir de `DataModelSettings.MnemonicDBPath` (buscar `MnemonicDBPath`), llamar primero `LegacyDataDetector.ResetIfRequested(settings.ArchiveLocations[0].ToPath(fs), settings.MnemonicDBPath.ToPath(fs), fs)`.

- [ ] **Step 4: Correr** → `Passed!`.

- [ ] **Step 5: Commit** — `git commit -am "feat: storage actions for legacy cleanup (move old downloads, Proton prefix, reset on restart)"`

## Task 15: Overlay del asistente

**Files:**
- Create: `src/NexusMods.App.UI/Overlays/LegacyCleanup/ILegacyCleanupOverlayViewModel.cs`, `LegacyCleanupOverlayViewModel.cs`, `LegacyCleanupOverlayView.axaml`, `LegacyCleanupOverlayView.axaml.cs`
- Modify: `src/NexusMods.App.UI/Services.cs` (registro View/VM, junto a `WelcomeOverlayView` línea ~232)
- Modify: `src/NexusMods.App.UI/Windows/MainWindowViewModel.cs:~93`

**Interfaces:**
- Consumes: `LegacyDataDetector` (Task 12), `IStorageAnalyzer` acciones (Tasks 11, 14), `ISynchronizerService`/tool manager vía `IStorageAnalyzer.RunDeepCleanOnAllLoadoutsAsync`, `IOSInterop.OpenUrl` (usado por `WelcomeOverlayViewModel` para links).
- Produces: `LegacyCleanupOverlayViewModel.CreateIfNeeded(IServiceProvider) → ILegacyCleanupOverlayViewModel?`

- [ ] **Step 1: VM**

`ILegacyCleanupOverlayViewModel.cs`:

```csharp
using R3;

namespace NexusMods.App.UI.Overlays;

public interface ILegacyCleanupOverlayViewModel : IOverlayViewModel
{
    BindableReactiveProperty<int> Step { get; }                 // 1..4
    BindableReactiveProperty<string> LegacyDownloadsText { get; }
    BindableReactiveProperty<bool> DeleteProtonPrefix { get; }
    BindableReactiveProperty<bool> IsBusy { get; }
    ReactiveCommand<Unit> CommandNext { get; }
    ReactiveCommand<Unit> CommandQuit { get; }
    ReactiveCommand<Unit> CommandVerifySteam { get; }
}
```

`LegacyCleanupOverlayViewModel.cs`:

```csharp
using Humanizer;
using Microsoft.Extensions.DependencyInjection;
using NexusMods.DataModel;
using NexusMods.DataModel.LegacyData;
using NexusMods.DataModel.Storage;
using NexusMods.Paths;
using NexusMods.Sdk;
using NexusMods.Sdk.Settings;
using R3;

namespace NexusMods.App.UI.Overlays;

public class LegacyCleanupOverlayViewModel : AOverlayViewModel<ILegacyCleanupOverlayViewModel>, ILegacyCleanupOverlayViewModel
{
    public BindableReactiveProperty<int> Step { get; } = new(1);
    public BindableReactiveProperty<string> LegacyDownloadsText { get; } = new("");
    public BindableReactiveProperty<bool> DeleteProtonPrefix { get; } = new(false);
    public BindableReactiveProperty<bool> IsBusy { get; } = new(false);
    public ReactiveCommand<Unit> CommandNext { get; }
    public ReactiveCommand<Unit> CommandQuit { get; }
    public ReactiveCommand<Unit> CommandVerifySteam { get; }

    public LegacyCleanupOverlayViewModel(IStorageAnalyzer storage, IOSInterop os, IFileSystem fs, Func<AbsolutePath?> steamLibraryRoot, Action restart, Action quit)
    {
        CommandQuit = new ReactiveCommand<Unit>(_ => quit());
        CommandVerifySteam = new ReactiveCommand<Unit>(_ => os.OpenUrl(new Uri("steam://validate/1091500")));
        CommandNext = IsBusy.Select(b => !b).ToReactiveCommand<Unit>(async (_, ct) =>
        {
            IsBusy.Value = true;
            try
            {
                switch (Step.Value)
                {
                    case 1:
                        var (count, size) = await storage.GetLegacyDownloadsAsync(ct);
                        LegacyDownloadsText.Value = count == 0
                            ? "No hay descargas viejas para rescatar."
                            : $"{count} archivos ({size.Value.Bytes().Humanize()}) en NexusMods.App/Downloads. Se mueven a tModManager/Downloads.";
                        break;
                    case 2:
                        await storage.MoveLegacyDownloadsAsync(ct);
                        break;
                    case 3:
                        await storage.RunDeepCleanOnAllLoadoutsAsync(ct);
                        if (DeleteProtonPrefix.Value && steamLibraryRoot() is { } root)
                            await storage.DeleteProtonPrefixAsync(root, ct);
                        break;
                    case 4:
                        LegacyDataDetector.RequestResetOnStart(fs);
                        restart();
                        return;
                }
                Step.Value++;
            }
            finally
            {
                IsBusy.Value = false;
            }
        }, awaitOperation: AwaitOperation.Drop);
    }

    public static ILegacyCleanupOverlayViewModel? CreateIfNeeded(IServiceProvider sp)
    {
        var fs = sp.GetRequiredService<IFileSystem>();
        var archives = sp.GetRequiredService<ISettingsManager>().Get<DataModelSettings>().ArchiveLocations[0].ToPath(fs);
        if (!LegacyDataDetector.HasNxArchives(archives)) return null;

        return new LegacyCleanupOverlayViewModel(
            sp.GetRequiredService<IStorageAnalyzer>(),
            sp.GetRequiredService<IOSInterop>(),
            fs,
            steamLibraryRoot: () => SteamLibraryRoot(sp),
            restart: () => sp.GetRequiredService<IRestartService>().Restart(),
            quit: () => Environment.Exit(0));
    }
}
```

Completar para el implementador:
- `SteamLibraryRoot(sp)`: la carpeta de la librería de Steam que contiene el juego = `GameInstallation.LocationsRegister[LocationId.Game]` del primer juego gestionado, subiendo hasta el directorio que contiene `steamapps` (el juego vive en `<lib>/steamapps/common/Cyberpunk 2077`). Devolver `null` si no hay juego gestionado.
- Reinicio: buscar cómo la app se reinicia hoy (`grep -rn "Restart" src/NexusMods.App.UI --include='*.cs' | grep -iv test`, por ejemplo tras cambiar settings con `RequiresRestart`). Usar ese mecanismo; si no existe, arrancar un nuevo proceso con `Environment.ProcessPath` y `Environment.Exit(0)`.
- Nombres exactos de `IOSInterop.OpenUrl`, `AOverlayViewModel`, `AwaitOperation`: copiar de `WelcomeOverlayViewModel` (mismo namespace).

- [ ] **Step 2: Vista** — `LegacyCleanupOverlayView.axaml`: mismo `Border` y estilos que `WelcomeOverlayView.axaml` (líneas 1-40 como plantilla). Contenido: un `TextBlock` título "Nueva forma de guardar tus mods" y cuatro `StackPanel` con `IsVisible` enlazado a `Step` (1..4):
  1. "tModManager ya no usa el formato .nx. Hay que empezar de cero: se borran la librería y los loadouts. Tus descargas se conservan."
  2. `LegacyDownloadsText`.
  3. "Se limpia la carpeta del juego (backup en tModManager/Backups). Después conviene verificar los archivos en Steam." + botón "Verificar integridad en Steam" (`CommandVerifySteam`) + `CheckBox` "Borrar también el prefix de Proton (se pierden saves no sincronizados con la nube y la configuración del prefix)" enlazado a `DeleteProtonPrefix`.
  4. "Listo. La app se reinicia y arranca limpia: gestioná el juego e instalá tu colección."
  Botones: "Salir" (`CommandQuit`) y "Siguiente" / "Reiniciar" en el paso 4 (`CommandNext`). Code-behind: `BindCommand` como en `WelcomeOverlayView.axaml.cs`, y `this.WhenAnyValue(v => v.ViewModel!.Step.Value)` para visibilidad.

- [ ] **Step 3: Registro y arranque** — en `Services.cs` agregar `.AddView<LegacyCleanupOverlayView, ILegacyCleanupOverlayViewModel>()`. En `MainWindowViewModel`, **antes** del welcome:

```csharp
            var legacyCleanup = LegacyCleanupOverlayViewModel.CreateIfNeeded(serviceProvider);
            if (legacyCleanup is not null)
            {
                overlayController.Enqueue(legacyCleanup);
                return; // nothing else runs until the user finishes or quits
            }
```

(Si `return` dentro del `WhenActivated` impide registrar disposables necesarios, en su lugar envolver el resto del bloque en `if (legacyCleanup is null) { ... }`.)

- [ ] **Step 4: Build** → 0/0. `RUN tests/NexusMods.UI.Tests` → sin fallos nuevos.

- [ ] **Step 5: Commit** — `git commit -am "feat: guided cleanup overlay for leftover .nx data"`

## Task 16: Prefix de Proton suelto en Storage Manager

**Files:**
- Modify: `StorageManagerPageViewModel.cs` (+ interfaz y vista)

- [ ] **Step 1:** Agregar `DeleteProtonPrefixCommand`, mismo patrón que `DeleteDownloadsCommand` (Task 11 Step 2). Texto del diálogo: "Se borra steamapps/compatdata/1091500. Steam lo recrea al lanzar el juego. Se pierden los saves que no estén sincronizados con la nube y toda la configuración del prefix." La raíz de la librería de Steam se resuelve igual que `SteamLibraryRoot` en Task 15: extraer esa función a un helper estático compartido en `NexusMods.App.UI` (por ejemplo `SteamPaths.LibraryRoot(IServiceProvider)`) y usarlo en ambos lugares.
- [ ] **Step 2:** Build 0/0; `RUN tests/NexusMods.UI.Tests` sin fallos nuevos.
- [ ] **Step 3:** Commit — `git commit -am "feat: delete Proton prefix from Storage Manager"`

## Task 17: Docs, suite completa y PR 3

**Files:**
- Modify: `CLAUDE.md`, `TODO.md`, `docs/superpowers/specs/2026-09-24-remove-nx-design.md` (estado → implementado)

- [ ] **Step 1: `CLAUDE.md`** — en "Fork-Specific Features":
  - 2 (Deep Clean): backups en `tModManager/Backups`, rutas nuevas, archivos sueltos no vanilla vía `IFileHashesService`.
  - 3 (Storage Manager): `DeleteDownloadsAsync` separado, prefix de Proton, acciones de limpieza de datos viejos.
  - 5 (MD5 rescan): la reextracción es `IDownloadReExtractor` desde `LibraryFile.DownloadPath`; borrar la mención a `.nx`.
  - 7 (App isolation): "Downloads: `tModManager/Downloads` (`DownloadsSettings`), ya no compartida".
  - En "Data Directory"/"Downloads": actualizar.
  - Nueva línea en Architecture/DataModel: "`LooseFileStore`: store por hash (`Archives/ab/<hash>`), GC por barrido (`LiveHashes`)".
  - Cantidad de proyectos: 78 → 72 (−3 src, −3 tests); verificar con `grep -c 'Project("' NexusMods.App.sln`.
- [ ] **Step 2: `TODO.md`** — marcar `[x] Eliminar .nx file store` con fecha y PRs; en "Errores conocidos", marcar los ítems de Deep Clean y readmes-en-raíz cubiertos (los readmes quedan cubiertos por Deep Clean; que los instaladores no los deployen sigue pendiente); borrar `NexusMods.Archives.Nx` de la sección de dependencias de Nexus.
- [ ] **Step 3: Suite completa vs `main`** (log `$S/ci-pr3.log`) → sin fallos nuevos; build 0/0.
- [ ] **Step 4: Commit + PR 3** con el checklist punta a punta del spec (§4) en el cuerpo:

```bash
git commit -am "docs: CLAUDE.md and TODO for the .nx removal"
git push -u origin feat/legacy-cleanup-wizard
gh pr create --base feat/downloads-first-class --title "feat: guided cleanup of .nx data, stronger Deep Clean, Proton prefix reset" --body "Asistente para datos .nx viejos (rescatar descargas, limpiar juego, verificar en Steam, prefix de Proton opcional, reset al reiniciar), Deep Clean reforzado, backups en tModManager/Backups. PR 3 de 3. Incluir el checklist punta a punta del spec §4."
```

- [ ] **Step 5: Handoff** — avisar a Tatoh: los tres PRs están listos; no publicar nada hasta correr el checklist en su Linux; orden de merge #PR1 → #PR2 → #PR3.
